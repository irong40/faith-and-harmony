---
phase: vapi-setup (pre-roadmap)
task: n8n-workflow-debug
status: in_progress
last_updated: 2026-03-03T14:19:06.768Z
---

<current_state>
VAPI voice bot setup is 95% complete. Two assistants are live with phone numbers provisioned. The n8n webhook workflow is imported into the DB and the webhook endpoint is responding (200 OK), but the workflow execution errors after the Extract Call Data node. The Supabase nodes were rewritten as httpRequest nodes to match the pattern used by working Sentinel workflows. Phone numbers updated across both codebases and deployed.
</current_state>

<completed_work>
- VAPI assistants created: Sentinel (757) 843-8772, F&H (757) 843-8458
- lookup_customer tool wired to vapi-tool-handler edge function (deployed)
- vapi_call_logs table + enhancements migration applied
- n8n webhook workflow JSON rewritten: Switch->If node, Supabase->httpRequest, Resend via httpRequest
- Workflow imported to n8n DB with full version/webhook records (custom SQL import pattern)
- Webhook live at https://n8n.faithandharmonyllc.com/webhook/vapi-calls (responds 200)
- Phone number 760.575.4876 replaced with 757.843.8772 across sentinel-landing and FaithandHarmony
- sentinel-landing committed 600b7f7 and pushed
- FaithandHarmony committed ca2586d and pushed
- n8n API key created: n8n_api_38c13405b553550cb0e980d725ccf3e81082fd8e0a16bf15
</completed_work>

<remaining_work>
1. Debug n8n workflow execution error — errors after Extract Call Data, likely at Lookup Customer by Phone httpRequest node. Check expression syntax for Supabase REST query params. Open n8n UI execution log for error details.
2. Add RESEND_API_KEY to D:\n8n\.env and restart container. The email notification node reads $env.RESEND_API_KEY.
3. Test both phone lines end-to-end: call (757) 843-8772 and (757) 843-8458, verify data flows to Supabase.
4. Wire Supabase credentials on n8n nodes if httpRequest approach doesn't work (fallback).
</remaining_work>

<decisions_made>
- Replaced all n8n Supabase nodes with httpRequest nodes using $env.SUPABASE_URL + $env.SUPABASE_SERVICE_KEY (no Supabase credential exists in n8n, this matches working Sentinel workflow pattern)
- Replaced Switch node v3.2 with If node v2 (Switch incompatible with n8n 2.9.0)
- Downgraded webhook typeVersion from 2 to 1.1 (matches working workflows)
- Replaced SMTP email node with httpRequest to Resend API using env var (no credential dependency)
- n8n workflow imported via direct SQL to postgres (CLI import misses workflow_history, webhook_entity, shared_workflow tables needed for UI and webhook registration)
</decisions_made>

<blockers>
- n8n execution error after Extract Call Data — need to check execution log in UI for specifics
- RESEND_API_KEY not in n8n container env — email notifications won't fire until added
- n8n API key may need rehashing — inserted raw into DB, authentication via header returned 401
</blockers>

<context>
The n8n workflow import was hard fought. n8n CLI import only writes to workflow_entity table but the UI needs workflow_history + shared_workflow + webhook_entity records to render nodes and register webhooks. Built a custom SQL import script (D:\n8n\workflows\import-vapi.js) that generates the full INSERT sequence with FK ordering. This pattern works and should be reused for future n8n workflow imports.

The execution error is likely a simple expression syntax issue in the httpRequest query parameters for the Supabase REST API call. The Supabase REST API expects filter params in a specific format (e.g., phone=ilike.%1234567890) that may not translate directly from the n8n expression syntax.
</context>

<next_action>
Start with: Open n8n UI at http://127.0.0.1:5678, find VAPI Call Webhook workflow, look at execution #5688 error details. The error is at or after the "Lookup Customer by Phone" httpRequest node. Fix the expression syntax, then retest with the same curl command from the handoff notes.
</next_action>
