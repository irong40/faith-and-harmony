---
phase: 01-intake-api-and-lead-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260303100000_create_leads_table.sql
  - supabase/migrations/20260303100001_vapi_call_logs_phase1.sql
  - supabase/migrations/20260303100002_quote_requests_source.sql
autonomous: true
requirements: [INTAKE-01, INTAKE-02]

must_haves:
  truths:
    - "A leads table exists with caller identity, source channel, qualification status, and FK links to clients and quote_requests"
    - "vapi_call_logs has transcript (text), duration_seconds (integer), sentiment, and outcome columns for complete call data storage"
    - "quote_requests has a source column defaulting to web and email allows null values"
    - "Dangling FK columns on vapi_call_logs referencing nonexistent tables are cleaned up"
  artifacts:
    - path: "supabase/migrations/20260303100000_create_leads_table.sql"
      provides: "Leads table with RLS and indexes"
      contains: "CREATE TABLE IF NOT EXISTS public.leads"
    - path: "supabase/migrations/20260303100001_vapi_call_logs_phase1.sql"
      provides: "sentiment, outcome columns and FK cleanup on vapi_call_logs"
      contains: "ADD COLUMN IF NOT EXISTS sentiment"
    - path: "supabase/migrations/20260303100002_quote_requests_source.sql"
      provides: "source column and relaxed email constraint on quote_requests"
      contains: "ADD COLUMN IF NOT EXISTS source"
  key_links:
    - from: "supabase/migrations/20260303100000_create_leads_table.sql"
      to: "public.clients"
      via: "FK reference"
      pattern: "REFERENCES public\\.clients"
    - from: "supabase/migrations/20260303100000_create_leads_table.sql"
      to: "public.quote_requests"
      via: "FK reference"
      pattern: "REFERENCES public\\.quote_requests"
---

<objective>
Create the database schema foundation for Phase 1 intake pipeline.

Purpose: The intake edge function (Plan 03) needs a leads table, sentiment/outcome columns on vapi_call_logs, and a source column on quote_requests before it can insert records. These migrations also clean up dangling FK columns from an earlier exploratory migration.
Output: Three new Supabase migrations ready to push.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-api-and-lead-tracking/01-RESEARCH.md
@supabase/migrations/20260301191812_vapi_call_logs.sql
@supabase/migrations/20260301193705_vapi_call_logs_enhancements.sql
@supabase/migrations/20260227100000_repair_quote_tables.sql
@supabase/migrations/20260224120000_create_clients_table.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migrations for leads, call log enhancements, and quote request source tracking</name>
  <files>
    supabase/migrations/20260303100000_create_leads_table.sql
    supabase/migrations/20260303100001_vapi_call_logs_phase1.sql
    supabase/migrations/20260303100002_quote_requests_source.sql
  </files>
  <action>
    Create three migration files following the project migration pattern (see 01-RESEARCH.md for exact SQL).

    FIRST: Verify INTAKE-02 column coverage on vapi_call_logs. The original migration (20260301191812) already defines `transcript text` and `duration_seconds integer`. Confirm these columns exist by reading that migration file. If for any reason they are missing from the applied schema, add them via ADD COLUMN IF NOT EXISTS in Migration 2 below. The expected state after all migrations: vapi_call_logs has transcript, duration_seconds, sentiment, outcome, and lead_id columns (transcript and duration from the original migration, the rest from this phase).

    Migration 1: `20260303100000_create_leads_table.sql`
    Create the `public.leads` table with these columns:
    - `id` uuid PK with gen_random_uuid()
    - `created_at` timestamptz DEFAULT now() NOT NULL
    - `updated_at` timestamptz DEFAULT now() NOT NULL
    - `caller_name` text NOT NULL
    - `caller_phone` text NOT NULL
    - `caller_email` text (nullable, voice calls may not capture email)
    - `source_channel` text NOT NULL DEFAULT 'voice_bot' (values: voice_bot, web, manual)
    - `call_id` text (nullable, links to vapi_call_logs.call_id)
    - `qualification_status` text NOT NULL DEFAULT 'pending' (values: pending, qualified, declined, transferred, no_answer)
    - `client_id` uuid FK to public.clients(id)
    - `quote_request_id` uuid FK to public.quote_requests(id)
    Enable RLS. Create service_role_all policy. Create admin read policy using `profiles.role = 'admin'` (matching existing quotes pattern, use only 'admin' not 'super_admin' per research recommendation). Create indexes on call_id, client_id, and (qualification_status, created_at DESC). Add table comment.

    Migration 2: `20260303100001_vapi_call_logs_phase1.sql`
    First, DROP the dangling FK columns that reference nonexistent tables:
    - `ALTER TABLE public.vapi_call_logs DROP COLUMN IF EXISTS customer_id;`
    - `ALTER TABLE public.vapi_call_logs DROP COLUMN IF EXISTS service_request_id;`
    Then ADD new columns (using ADD COLUMN IF NOT EXISTS for safety):
    - `transcript text` (defensive re-add, already exists from original migration but ensures INTAKE-02 compliance)
    - `duration_seconds integer DEFAULT 0` (defensive re-add, same rationale)
    - `sentiment text` (values: positive, neutral, negative, unknown)
    - `outcome text` (values: qualified, declined, transferred, voicemail, abandoned)
    - `lead_id uuid REFERENCES public.leads(id)` (correct FK to the new leads table)
    Add column comments. Create index on lead_id WHERE lead_id IS NOT NULL.

    Migration 3: `20260303100002_quote_requests_source.sql`
    - `ALTER TABLE public.quote_requests ADD COLUMN IF NOT EXISTS source text NOT NULL DEFAULT 'web';`
    - `ALTER TABLE public.quote_requests ALTER COLUMN email DROP NOT NULL;`
    - Create index on (source, created_at DESC).
    - Add column comment for source.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx supabase migration list 2>&1 | tail -5</automated>
    <manual>Verify three new migration files exist and have correct timestamps that sort after all existing migrations</manual>
  </verify>
  <done>Three migration SQL files exist in supabase/migrations/ with correct schema definitions, RLS policies, indexes, and comments. The leads table has FKs to clients and quote_requests. vapi_call_logs has transcript, duration_seconds, sentiment, outcome, and lead_id columns with dangling customer_id and service_request_id dropped. quote_requests has source column and email allows null.</done>
</task>

<task type="auto">
  <name>Task 2: Push migrations to Supabase and verify schema including INTAKE-02 transcript and duration columns</name>
  <files>
    supabase/migrations/20260303100000_create_leads_table.sql
    supabase/migrations/20260303100001_vapi_call_logs_phase1.sql
    supabase/migrations/20260303100002_quote_requests_source.sql
  </files>
  <action>
    Push all three migrations to the remote Supabase project using `npx supabase db push`. If any migration fails due to FK or constraint issues, diagnose and fix the SQL before retrying.

    After push succeeds, verify the schema state by running SQL queries against the database:
    1. Verify leads table exists with expected columns: `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'leads' ORDER BY ordinal_position;`
    2. Verify vapi_call_logs has ALL INTAKE-02 required columns (transcript, duration_seconds, sentiment, outcome) plus lead_id, and the dangling customer_id and service_request_id are gone: `SELECT column_name FROM information_schema.columns WHERE table_name = 'vapi_call_logs' ORDER BY ordinal_position;`
    3. Verify quote_requests has source column and email is nullable: `SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name = 'quote_requests' AND column_name IN ('source', 'email');`

    Specific INTAKE-02 verification: Confirm vapi_call_logs output includes ALL of these columns: call_id, transcript, duration_seconds, sentiment, outcome, lead_id. If any are missing after push, create a corrective migration immediately.

    Use `npx supabase db execute` or the project's Supabase SQL interface for verification queries.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx supabase migration list 2>&1 | grep -c "20260303"</automated>
    <manual>All three migrations show as applied. vapi_call_logs has transcript, duration_seconds, sentiment, outcome, and lead_id columns. No errors in push output.</manual>
  </verify>
  <done>All three migrations applied to remote Supabase. leads table queryable. vapi_call_logs has transcript, duration_seconds, sentiment, outcome, lead_id columns (satisfying INTAKE-02 fully). quote_requests has source column with email nullable. No dangling FK columns remain on vapi_call_logs.</done>
</task>

</tasks>

<verification>
Run `npx supabase migration list` and confirm all three new migrations appear as applied. Run a test insert into leads (via Supabase dashboard SQL editor or CLI) to confirm FK constraints work:
```sql
INSERT INTO public.leads (caller_name, caller_phone, source_channel, qualification_status) VALUES ('Test Lead', '7575551234', 'voice_bot', 'pending');
DELETE FROM public.leads WHERE caller_name = 'Test Lead';
```
Verify INTAKE-02 completeness by querying vapi_call_logs columns and confirming transcript, duration_seconds, sentiment, and outcome all exist.
</verification>

<success_criteria>
1. leads table exists with all specified columns, RLS enabled, service_role and admin policies active
2. vapi_call_logs has transcript, duration_seconds, sentiment, outcome, lead_id columns; customer_id and service_request_id columns removed
3. quote_requests has source column (default 'web') and email column is nullable
4. All three migrations listed as applied in `supabase migration list`
5. INTAKE-02 fully satisfied: call_id, transcript, duration_seconds, sentiment, and outcome all present on vapi_call_logs
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-api-and-lead-tracking/01-01-SUMMARY.md`
</output>
