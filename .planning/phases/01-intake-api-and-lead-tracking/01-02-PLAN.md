---
phase: 01-intake-api-and-lead-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/pricing-lookup/index.ts
autonomous: true
requirements: [INTAKE-04]

must_haves:
  truths:
    - "A GET request with service_type=re_basic returns Listing Lite at $225 with correct deliverables"
    - "A GET request without service_type returns all 6 packages"
    - "A GET request with an unknown service_type returns 404 with error message"
    - "All prices match CLAUDE.md canonical values exactly"
  artifacts:
    - path: "supabase/functions/pricing-lookup/index.ts"
      provides: "Static pricing lookup edge function"
      min_lines: 50
      exports: ["serve handler"]
  key_links:
    - from: "supabase/functions/pricing-lookup/index.ts"
      to: "CLAUDE.md pricing section"
      via: "hardcoded PACKAGES constant"
      pattern: "re_basic.*225|re_standard.*450|re_premium.*750"
---

<objective>
Create the pricing lookup edge function that returns package pricing and deliverables for mid-call bot queries.

Purpose: During a voice call, the Vapi bot needs to query real-time pricing data. This read-only edge function serves as that data source. It requires no database access because prices are canonical locked values from CLAUDE.md.
Output: Deployed edge function at /pricing-lookup accepting GET requests.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-api-and-lead-tracking/01-RESEARCH.md
@supabase/functions/mission-control-api/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing-lookup edge function with canonical package data</name>
  <files>supabase/functions/pricing-lookup/index.ts</files>
  <action>
    Create `supabase/functions/pricing-lookup/index.ts` following the exact edge function pattern from `mission-control-api/index.ts` (see 01-RESEARCH.md Pattern 1 and Pattern 4).

    The function:
    1. Handles OPTIONS preflight with standard CORS headers (same as every other function in the project).
    2. Accepts GET requests only. Returns 405 for other methods.
    3. Reads `service_type` from query string parameters.
    4. If `service_type` is provided, returns that single package or 404 if unknown.
    5. If `service_type` is omitted, returns all 6 packages.

    Hardcode the PACKAGES constant with these exact values from CLAUDE.md:
    - re_basic: Listing Lite, $225, deliverables: 10 photos, Sky replacement, Next day delivery
    - re_standard: Listing Pro, $450, deliverables: 25 photos, 60 second reel, 2D boundary overlay, 48 hour turnaround
    - re_premium: Luxury Listing, $750, deliverables: 40+ photos, 2 minute cinematic video, Twilight shoot, 24 hour priority
    - construction: Construction Progress, $450/visit, deliverables: Orthomosaic, Site overview, Date stamped archive
    - commercial: Commercial Marketing, $850, deliverables: 4K video, 3D model, Raw footage, Perpetual license
    - inspection: Inspection Data, $1200, deliverables: Inspection grid photography, Annotated report, Exportable data

    Also include add-ons in a separate object:
    - rush_24h: Rush Premium 24 hour, modifier +25%
    - rush_same_day: Rush Premium same day, modifier +50%
    - raw_buyout: Raw File Buyout, flat fee $250
    - brokerage_retainer: Brokerage Retainer, $1500/month for 5 Listing Pro shoots

    When returning a single package, include `service_type` in the response alongside name, price, unit (if applicable), and deliverables.
    When returning all packages, return `{ packages: {...}, add_ons: {...} }`.

    No authentication required (this is a read-only public endpoint, same as how Vapi tool calls work). No Supabase client needed (no DB access).

    Use the project import style:
    ```typescript
    import { serve } from 'https://deno.land/std@0.190.0/http/server.ts';
    ```

    Create a `json()` helper function for consistent response formatting (same pattern as mission-control-api).
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && test -f supabase/functions/pricing-lookup/index.ts && echo "EXISTS" || echo "MISSING"</automated>
    <manual>Verify the file contains all 6 packages with correct prices matching CLAUDE.md</manual>
  </verify>
  <done>pricing-lookup/index.ts exists with all 6 packages, 4 add-ons, CORS handling, GET-only enforcement, and both single-package and all-packages response paths.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy pricing-lookup and verify responses</name>
  <files>supabase/functions/pricing-lookup/index.ts</files>
  <action>
    Deploy the edge function using `npx supabase functions deploy pricing-lookup --no-verify-jwt`.

    The `--no-verify-jwt` flag is required because this function is called by Vapi tool calls during a voice conversation. There is no Supabase user session in that context. This matches the public nature of the pricing data (already displayed on the landing page).

    After deployment, test three scenarios using curl against the deployed function URL:

    1. Single package lookup:
       `curl -s "https://qjpujskwqaehxnqypxzu.supabase.co/functions/v1/pricing-lookup?service_type=re_basic"`
       Expected: JSON with name "Listing Lite", price 225, and 3 deliverables.

    2. All packages:
       `curl -s "https://qjpujskwqaehxnqypxzu.supabase.co/functions/v1/pricing-lookup"`
       Expected: JSON with packages object containing 6 entries and add_ons object.

    3. Unknown service type:
       `curl -s "https://qjpujskwqaehxnqypxzu.supabase.co/functions/v1/pricing-lookup?service_type=unknown"`
       Expected: 404 with error message.
  </action>
  <verify>
    <automated>curl -s "https://qjpujskwqaehxnqypxzu.supabase.co/functions/v1/pricing-lookup?service_type=re_standard" 2>&1 | grep -c "450"</automated>
    <manual>Verify re_standard returns $450 with "Listing Pro" name and 4 deliverables</manual>
  </verify>
  <done>pricing-lookup deployed and responding. Single package returns correct data. All packages returns 6 entries plus add-ons. Unknown type returns 404. No JWT required for access.</done>
</task>

</tasks>

<verification>
Test all 6 package codes return correct prices:
```bash
for code in re_basic re_standard re_premium construction commercial inspection; do
  echo "$code: $(curl -s "https://qjpujskwqaehxnqypxzu.supabase.co/functions/v1/pricing-lookup?service_type=$code" | grep -o '"price":[0-9]*')"
done
```
Expected output: re_basic:225, re_standard:450, re_premium:750, construction:450, commercial:850, inspection:1200
</verification>

<success_criteria>
1. pricing-lookup edge function deployed and accessible without JWT
2. GET with service_type returns correct package name, price, and deliverables
3. GET without service_type returns all 6 packages plus add-ons
4. GET with unknown service_type returns 404
5. All prices match CLAUDE.md canonical values exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-api-and-lead-tracking/01-02-SUMMARY.md`
</output>
