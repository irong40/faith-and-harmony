---
phase: 01-seo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main.tsx
  - src/App.tsx
  - index.html
  - src/pages/LandingPage.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-11

must_haves:
  truths:
    - "View-source on the landing page shows title 'Drone Photography & Aerial Inspections | Hampton Roads VA', not 'Trestle'"
    - "Meta description contains veteran owned, LAANC authorized, 48 hour turnaround, and Hampton Roads"
    - "Canonical URL tag is present in the document head pointing to the production domain"
    - "react-helmet-async is installed and HelmetProvider wraps the app"
  artifacts:
    - path: "src/components/seo/LandingPageHelmet.tsx"
      provides: "Per-route Helmet component for the landing page"
      exports: ["LandingPageHelmet"]
    - path: "src/components/seo/DefaultHelmet.tsx"
      provides: "Default Helmet for all other routes (restores Trestle identity for pilots)"
      exports: ["DefaultHelmet"]
  key_links:
    - from: "src/main.tsx"
      to: "HelmetProvider"
      via: "wrap of root App"
      pattern: "HelmetProvider"
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/seo/LandingPageHelmet.tsx"
      via: "component import and render"
      pattern: "LandingPageHelmet"
---

<objective>
Install react-helmet-async and replace the hardcoded index.html title and meta tags with a HelmetProvider at the root and per-route Helmet components. The landing page gets Sentinel-specific identity. All other routes continue to present Trestle identity via a DefaultHelmet fallback.

Purpose: Google currently sees "Trestle field operations command center" when it crawls the landing page. This plan fixes the page identity at the React level so the correct title and description render in every crawl and social share.

Output: react-helmet-async installed, HelmetProvider in main.tsx, LandingPageHelmet component with correct title/description/canonical, DefaultHelmet component preserving Trestle identity for pilot and admin routes.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/.planning/STATE.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/package.json
@D:/Projects/FaithandHarmony/src/main.tsx
@D:/Projects/FaithandHarmony/src/App.tsx
@D:/Projects/FaithandHarmony/index.html
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-helmet-async and wrap app root</name>
  <files>
    package.json
    src/main.tsx
  </files>
  <action>
    Install react-helmet-async:
    ```
    npm install react-helmet-async
    ```

    In src/main.tsx, wrap the App component with HelmetProvider. Import HelmetProvider from 'react-helmet-async'. The HelmetProvider must be the outermost wrapper so Helmet components anywhere in the tree can set document head tags.

    Current main.tsx structure wraps App in StrictMode. The final order should be:
    StrictMode > HelmetProvider > App

    Also update index.html to remove the hardcoded title and meta description (these will be managed by react-helmet-async at runtime). Replace the title tag with a neutral placeholder: `<title>Sentinel Aerial Inspections</title>`. Remove the og:title, og:description, og:type, and twitter:card meta tags from index.html as they will be rendered by Helmet. Keep charset, viewport, author, theme-color, apple-mobile-web-app-*, and font link tags in index.html since those are non-route-specific.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>Confirm no TypeScript errors on HelmetProvider import. Confirm index.html no longer has "Trestle" in the title.</manual>
  </verify>
  <done>react-helmet-async is in package.json dependencies. HelmetProvider wraps App in main.tsx. index.html title no longer reads "Trestle".</done>
</task>

<task type="auto">
  <name>Task 2: Create LandingPageHelmet and DefaultHelmet components</name>
  <files>
    src/components/seo/LandingPageHelmet.tsx
    src/components/seo/DefaultHelmet.tsx
    src/pages/LandingPage.tsx
    src/App.tsx
  </files>
  <action>
    Create src/components/seo/LandingPageHelmet.tsx:

    This component renders a Helmet with Sentinel landing page identity. Use the following exact values:

    - title: "Drone Photography & Aerial Inspections | Hampton Roads VA"
    - meta description: "Veteran owned drone services in Hampton Roads VA. FAA Part 107 certified, LAANC authorized for military airspace, 48 hour turnaround. Aerial photography, property inspections, and 3D photogrammetry for real estate professionals and commercial contractors."
    - canonical: "https://sentinelaerial.faithandharmonyllc.com" (the production URL, read from a VITE_PUBLIC_URL env var with this as fallback)
    - meta name="robots" content="index, follow"

    Note: Open Graph and Twitter card tags are handled in plan 01-03. Do not add og:* or twitter:* tags in this component — they are added there.

    Create src/components/seo/DefaultHelmet.tsx:

    This component renders the Trestle identity for authenticated app routes:
    - title: "Trestle — Sentinel Aerial Inspections"
    - meta name="robots" content="noindex, nofollow"

    The noindex prevents the Trestle pilot/admin interface from competing with the landing page in search results.

    In src/pages/LandingPage.tsx: import LandingPageHelmet from '@/components/seo/LandingPageHelmet' and render it as the first child inside the return, before the landing-page div.

    In src/App.tsx: In the RootRedirect component, when user is null (renders LandingPage), no change needed since LandingPage now includes LandingPageHelmet. For admin and pilot routes, add DefaultHelmet at the top of the main Routes tree. Wrap the entire Routes block with a Fragment containing DefaultHelmet first, so any route that does not have its own Helmet falls back to Trestle identity. LandingPage's own LandingPageHelmet will override DefaultHelmet due to react-helmet-async's last-wins precedence.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build 2>&1 | tail -5</automated>
    <manual>Run npm run dev. Open http://localhost:8080. Check browser tab — should read "Drone Photography & Aerial Inspections | Hampton Roads VA". Check view-source and confirm title tag matches.</manual>
  </verify>
  <done>Browser tab on the landing page shows the correct Sentinel title. TypeScript compiles without errors. Build succeeds.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification sequence:

1. `npm run typecheck` passes with no errors
2. `npm run build` completes without errors
3. `npm run dev` serves on localhost:8080
4. Browser tab title on the root route reads "Drone Photography & Aerial Inspections | Hampton Roads VA"
5. View-source of the rendered HTML (not the React source) shows the title and meta description via react-helmet-async output
6. Navigating to /auth or /admin/dashboard shows the browser tab reading "Trestle — Sentinel Aerial Inspections"
</verification>

<success_criteria>
- react-helmet-async installed and in package.json
- HelmetProvider wraps the app root in src/main.tsx
- LandingPage renders with title "Drone Photography & Aerial Inspections | Hampton Roads VA"
- Meta description contains "veteran owned", "Hampton Roads", "LAANC", "48 hour turnaround"
- Canonical link tag present on landing page pointing to production URL
- Admin and pilot routes show Trestle title with noindex robots meta
- npm run typecheck passes
- npm run build passes
</success_criteria>

<output>
After completion, create .planning/phases/01-seo-foundation/01-01-SUMMARY.md with:
- What was installed and configured
- The canonical URL value used
- Any deviations from this plan
- Files created and modified
</output>
