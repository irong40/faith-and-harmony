---
phase: 01-seo-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/pages/LandingPage.tsx
autonomous: true
requirements:
  - HTML-01
  - HTML-02
  - HTML-03
  - HTML-04
  - HTML-05
  - HTML-06

must_haves:
  truths:
    - "The landing page DOM contains a main element wrapping all page sections"
    - "The header element contains a nav element wrapping the navigation links"
    - "Every section element has an aria-label attribute"
    - "The H1 tag contains the primary keyword phrase, not the brand name"
    - "The hero H2 becomes the brand name display and H1 gets the keyword headline"
    - "All img tags have descriptive alt text and explicit width and height attributes"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Semantically correct landing page with main, nav, aria-labels, corrected H1, and image attributes"
  key_links:
    - from: "header"
      to: "nav"
      via: "nav wrapping navigation links inside header element"
      pattern: "<nav"
    - from: "H1"
      to: "keyword phrase"
      via: "H1 containing drone services keyword, not SENTINEL AERIAL INSPECTIONS brand name"
      pattern: "Professional Drone"
---

<objective>
Apply semantic HTML corrections to LandingPage.tsx. These are structural changes only. No text content, styling, or behavior changes. The corrections address: missing main element, missing nav inside header, missing aria-labels on sections, H1 on brand name instead of keyword, H2 in hero on keyword instead of brand, and img tags missing width/height.

Purpose: Google uses semantic HTML to understand page structure. The current page has the H1 on the brand name (SENTINEL AERIAL INSPECTIONS) and the keyword phrase (PROFESSIONAL DRONE SERVICES HAMPTON ROADS) on an H2. For SEO the keyword phrase must be H1. Screen readers need aria-labels to navigate sections. The main element tells crawlers where page content begins.

Output: Updated LandingPage.tsx with corrected semantic structure. No visual change to the page.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add main element, nav inside header, and aria-labels on sections</name>
  <files>
    src/pages/LandingPage.tsx
  </files>
  <action>
    Read the current LandingPage.tsx. It has this structure at the top level:

    ```
    <div className="landing-page">
      <div className="lp-scanline-overlay" />
      <div className="lp-scanline-bar" />
      <div className="lp-grid-bg" />
      <header className="lp-header"> ... </header>
      <section className="lp-hero"> ... </section>
      <section className="lp-services"> ... </section>
      <section className="lp-equipment"> ... </section>
      <section className="lp-portfolio" id="portfolio"> ... </section>
      <section className="lp-about"> ... </section>
      <section className="lp-vets"> ... </section>
      <section className="lp-contact"> ... </section>
      <footer className="lp-footer"> ... </footer>
    </div>
    ```

    Changes to make in this task:

    1. Wrap all section and footer elements (everything after the header) in a `<main className="lp-main">` element. The header stays outside main. The lp-scanline-overlay, lp-scanline-bar, and lp-grid-bg decorative divs also stay outside main.

    2. Inside the header, the lp-header-right div contains a Link to /auth and a veteran badge div. Add a `<nav aria-label="Site navigation">` element wrapping the Link component (the Pilot Login link). The veteran badge div stays outside nav since it is not a navigation link. Structure becomes:
    ```tsx
    <div className="lp-header-right">
      <nav aria-label="Site navigation">
        <Link to="/auth" className="lp-login-link">Pilot Login</Link>
      </nav>
      <div className="lp-veteran-badge">100% VETERAN OWNED</div>
    </div>
    ```

    3. Add aria-label attributes to each section element:
    - lp-hero: aria-label="Hero"
    - lp-services: aria-label="Services"
    - lp-equipment: aria-label="Equipment"
    - lp-portfolio: aria-label="Portfolio"
    - lp-about: aria-label="About"
    - lp-vets: aria-label="Veterans"
    - lp-contact: aria-label="Contact"

    Do not change any text content, className values, or child structure in this task.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>Open http://localhost:8080, open DevTools Elements, confirm: (1) main element exists containing sections; (2) header contains a nav element; (3) each section has an aria-label attribute. Visual appearance of the page must be unchanged.</manual>
  </verify>
  <done>DOM contains main element, nav inside header, and aria-label on every section. TypeScript compiles without errors. Page appearance is identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Fix H1 keyword placement and add image width/height attributes</name>
  <files>
    src/pages/LandingPage.tsx
  </files>
  <action>
    This task makes two targeted changes to LandingPage.tsx. Do not modify anything outside the specific elements listed.

    Change 1: Swap H1 and H2 in the header and hero sections.

    Current state in header:
    ```tsx
    <h1>SENTINEL AERIAL INSPECTIONS</h1>
    ```

    Current state in hero section:
    ```tsx
    <h2>PROFESSIONAL DRONE SERVICES<span>HAMPTON ROADS</span></h2>
    ```

    Required state:
    - In the header logo section: change `<h1>` to `<p className="lp-logo-heading">` (this is a brand name display, not the primary keyword H1). This keeps the visual appearance by applying the same CSS class. Add the class lp-logo-heading to landing.css only if needed — check if lp-logo-heading styles already exist. If not, the executor should note this in the SUMMARY and add a minimal CSS rule: `.lp-logo-heading { font-size: inherit; font-weight: inherit; margin: 0; }` to src/pages/landing.css to preserve visual identity.
    - In the hero section: change `<h2>PROFESSIONAL DRONE SERVICES<span>HAMPTON ROADS</span></h2>` to `<h1 className="lp-hero-h1">PROFESSIONAL DRONE SERVICES<span>HAMPTON ROADS</span></h1>`. Add a CSS rule if lp-hero-h1 does not already exist in landing.css. The rule should inherit existing h2 styles: check what CSS the lp-hero h2 selector currently defines and mirror it to an h1 selector or add the class. The goal is zero visual change.
    - In the services section: `<h2 className="lp-section-title">OUR SERVICES</h2>` — this is already H2, no change needed.
    - In subsequent sections: Confirm all other section title h2 elements remain h2. Check heading hierarchy: H1 (hero keyword) > H2 (section titles) > H3 (card titles). No heading levels should be skipped.

    Change 2: Add width and height attributes to all img elements.

    Current img elements in the file:
    - `/assets/landing/sentinel-logo.png` alt="Sentinel Aerial Inspections Logo" — add width="200" height="60" (approximate, will be corrected in Phase 2 image optimization)
    - `/assets/landing/matrice-4e.png` alt="DJI Matrice 4E Enterprise Drone" — add width="600" height="400"
    - `/assets/landing/hero-banner.jpg` alt="DJI Matrice 4 Fleet" — add width="1200" height="630"

    Note: These are placeholder dimensions. Phase 2 will confirm actual image dimensions and update them. Setting any explicit width/height now eliminates layout shift and satisfies HTML-06. The executor must add the attributes even if approximate — do not skip this step waiting for Phase 2.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
    <manual>Open http://localhost:8080. Confirm: (1) The hero keyword text renders visually identical to before. (2) The header brand name "SENTINEL AERIAL INSPECTIONS" still appears the same size and weight as before. (3) DevTools shows H1 is in the hero section on the keyword phrase, not the brand name. (4) All three img elements have explicit width and height attributes.</manual>
  </verify>
  <done>H1 is on "PROFESSIONAL DRONE SERVICES HAMPTON ROADS" in the hero section. Brand name in header is no longer H1. All img elements have width and height attributes. Visual appearance unchanged. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `npm run build` passes
3. DOM has exactly one H1 element containing the drone services keyword phrase
4. header element contains a nav element
5. main element wraps all page sections
6. Every section element has an aria-label attribute (7 sections total)
7. All three img elements have explicit width and height attributes
8. Heading hierarchy: H1 > H2 (section titles) > H3 (card titles) with no skipped levels
9. No visual difference from pre-plan state when viewing the page in the browser
</verification>

<success_criteria>
- LandingPage.tsx passes TypeScript typecheck
- DOM structure: main wraps sections, header contains nav, 7 sections have aria-label
- H1 tag is on the keyword phrase in the hero section, not the brand name
- Brand name in header is demoted from H1 to a paragraph or span element with CSS class preserving visual style
- All img tags have width and height attributes
- Heading hierarchy is valid (H1 > H2 > H3, no skips)
- npm run build passes
</success_criteria>

<output>
After completion, create .planning/phases/01-seo-foundation/01-04-SUMMARY.md with:
- What element replaced the H1 brand name in the header (paragraph, span, etc.) and CSS class used
- Actual img dimensions used (for Phase 2 reference)
- List of all aria-label values applied per section
- Any deviations from this plan
</output>
