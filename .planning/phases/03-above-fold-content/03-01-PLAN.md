---
phase: 03-above-fold-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/StickyNav.tsx
  - src/pages/LandingPage.tsx
  - src/pages/landing.css
autonomous: true
requirements:
  - PAGE-01
  - CONV-02

must_haves:
  truths:
    - "A navigation bar appears fixed at the top of the viewport when the user scrolls down past the header"
    - "The sticky nav contains anchor links to each page section"
    - "The sticky nav contains a Get a Quote CTA button that scrolls to the quote form anchor"
    - "The phone number 760.575.4876 is visible in the sticky nav"
    - "The sticky nav is hidden at the top of the page and becomes visible after the user scrolls 80px"
  artifacts:
    - path: "src/components/landing/StickyNav.tsx"
      provides: "Sticky navigation component with scroll detection"
      min_lines: 60
    - path: "src/pages/landing.css"
      provides: "CSS for sticky nav hidden/visible states, layout, and CTA styling"
      contains: ".lp-sticky-nav"
  key_links:
    - from: "src/components/landing/StickyNav.tsx"
      to: "src/pages/LandingPage.tsx"
      via: "imported and rendered above the header element"
      pattern: "StickyNav"
    - from: "StickyNav.tsx"
      to: "window scroll event"
      via: "useEffect with addEventListener"
      pattern: "addEventListener.*scroll"
---

<objective>
Build a sticky navigation bar component that appears on scroll and gives visitors quick access to every section plus a direct Get a Quote CTA.

Purpose: Visitors who scroll past the hero need a persistent way to contact Sentinel or jump to pricing without scrolling back up. The sticky nav keeps conversion accessible at all times.
Output: StickyNav.tsx component, CSS for scroll-triggered visibility, LandingPage.tsx updated to include the component.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
@D:/Projects/FaithandHarmony/src/pages/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StickyNav component</name>
  <files>src/components/landing/StickyNav.tsx</files>
  <action>
    Create the directory src/components/landing/ if it does not exist.

    Create StickyNav.tsx as a React functional component using TypeScript.

    The component tracks scroll position with useEffect and addEventListener('scroll'). When window.scrollY exceeds 80, set a boolean state variable `visible` to true. When scrollY drops below 80, set it to false. Remove the event listener on cleanup.

    Render a nav element with className "lp-sticky-nav" and a conditional class "lp-sticky-nav--visible" when visible is true.

    Inside the nav render a div with className "lp-container" containing two children:

    Left side: a div with className "lp-sticky-nav__links" containing anchor links to the following section IDs:
    - href="#services" text "Services"
    - href="#pricing" text "Pricing"
    - href="#portfolio" text "Portfolio"
    - href="#contact" text "Contact"

    Right side: a div with className "lp-sticky-nav__actions" containing:
    - An anchor tag with href="tel:7605754876" className "lp-sticky-nav__phone" and text "760.575.4876"
    - An anchor tag with href="#quote" className "lp-cta-button lp-sticky-nav__cta" and text "Get a Quote"

    Writing constraints from CLAUDE.md apply to all text in JSX. No dashes in displayed text. Active voice. Short clear labels.

    The component does NOT use React Router Link for the anchor links because these are page section anchors, not routes. Use plain anchor tags.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify StickyNav.tsx exists at src/components/landing/StickyNav.tsx and compiles without TypeScript errors.</manual>
  </verify>
  <done>StickyNav.tsx exists, compiles without TypeScript errors, and contains the scroll detection logic with useEffect, the anchor links, phone number, and Get a Quote CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Add sticky nav CSS and wire into LandingPage</name>
  <files>src/pages/landing.css, src/pages/LandingPage.tsx</files>
  <action>
    In landing.css, append a new block of CSS for the sticky nav AFTER the existing footer styles. Do not modify existing CSS rules. All new selectors use the .landing-page scope prefix.

    Add these styles:

    .landing-page .lp-sticky-nav
    - position: fixed
    - top: 0
    - left: 0
    - right: 0
    - z-index: 2000
    - background: rgba(28, 28, 28, 0.97)
    - border-bottom: 1px solid var(--safety-orange)
    - backdrop-filter: blur(12px)
    - transform: translateY(-100%)
    - transition: transform 0.3s ease
    - padding: 0

    .landing-page .lp-sticky-nav.lp-sticky-nav--visible
    - transform: translateY(0)

    .landing-page .lp-sticky-nav .lp-container
    - display: flex
    - justify-content: space-between
    - align-items: center
    - padding-top: 14px
    - padding-bottom: 14px

    .landing-page .lp-sticky-nav__links
    - display: flex
    - gap: 32px
    - align-items: center

    .landing-page .lp-sticky-nav__links a
    - font-family: 'Share Tech Mono', monospace
    - font-size: 12px
    - letter-spacing: 2px
    - text-transform: uppercase
    - color: var(--steel-gray)
    - text-decoration: none
    - transition: color 0.2s

    .landing-page .lp-sticky-nav__links a:hover
    - color: var(--safety-orange)

    .landing-page .lp-sticky-nav__actions
    - display: flex
    - gap: 20px
    - align-items: center

    .landing-page .lp-sticky-nav__phone
    - font-family: 'Share Tech Mono', monospace
    - font-size: 13px
    - letter-spacing: 1px
    - color: var(--warm-white)
    - text-decoration: none
    - transition: color 0.2s

    .landing-page .lp-sticky-nav__phone:hover
    - color: var(--safety-orange)

    .landing-page .lp-sticky-nav .lp-sticky-nav__cta
    - padding: 10px 28px
    - font-size: 14px
    - animation: none

    In LandingPage.tsx:
    - Import StickyNav at the top: import StickyNav from '../components/landing/StickyNav'
    - Place the StickyNav component as the FIRST child inside the outermost div.landing-page, before the lp-scanline-overlay div.
    - Add id attributes to the existing sections so the sticky nav anchors work:
      - lp-services section: add id="services"
      - lp-portfolio section already has id="portfolio" (keep it)
      - lp-contact section: add id="contact"
      - Add id="pricing" to the lp-services section for now. Phase 3 plan 03 will replace this with a real pricing section. When the pricing section is added in plan 03, the id will move to that section. For now the services section carries the pricing anchor as a placeholder.

    Do not change any other existing JSX structure.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
    <manual>Run the dev server with npm run dev. Open the landing page. Scroll down 100px. Confirm the sticky nav slides into view from the top. Confirm anchor links and the Get a Quote button are visible. Scroll back to the top and confirm it hides again.</manual>
  </verify>
  <done>Sticky nav appears on scroll, hides at page top, shows phone number 760.575.4876 and Get a Quote CTA, and all section anchor links are present. TypeScript compiles and the Vite build succeeds.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors: npx tsc --noEmit
- Vite build succeeds: npm run build
- StickyNav.tsx exists at src/components/landing/StickyNav.tsx
- landing.css contains .lp-sticky-nav styles
- LandingPage.tsx imports and renders StickyNav
- Section IDs exist for services, pricing, portfolio, contact
</verification>

<success_criteria>
PAGE-01: A sticky navigation bar is visible on scroll with section anchor links and a Get a Quote CTA button.
CONV-02: Phone number 760.575.4876 is visible in the sticky nav (it will also be in the hero, added in plan 03-02).
</success_criteria>

<output>
After completion, create `.planning/phases/03-above-fold-content/03-01-SUMMARY.md` using the summary template.
</output>
