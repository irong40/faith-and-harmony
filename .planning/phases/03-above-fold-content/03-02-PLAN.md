---
phase: 03-above-fold-content
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/landing/HeroSection.tsx
  - src/components/landing/TrustBar.tsx
  - src/pages/LandingPage.tsx
  - src/pages/landing.css
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - CONV-01
  - CONV-02

must_haves:
  truths:
    - "The hero section H1 contains the keyword phrase 'Drone Photography and Aerial Inspections Hampton Roads'"
    - "A subheadline below the H1 addresses realtors and contractors by name"
    - "The phone number 760.575.4876 is visible in the hero section as a clickable tel link"
    - "The primary CTA above the fold reads 'Get a Quote' and links to #quote"
    - "A trust bar below the hero displays four badges: FAA Part 107, $1M Insurance, Veteran Owned, 48 Hour Turnaround"
    - "No CTA reads 'View Our Work' above the fold"
  artifacts:
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Hero section with H1, subheadline, phone, and Get a Quote CTA"
      min_lines: 50
    - path: "src/components/landing/TrustBar.tsx"
      provides: "Four trust badge strip below hero"
      min_lines: 30
    - path: "src/pages/landing.css"
      provides: "CSS for hero rebuild, phone link, and trust bar badges"
      contains: ".lp-trust-bar"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/landing/HeroSection.tsx"
      via: "import and render replacing the existing lp-hero section"
      pattern: "HeroSection"
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/landing/TrustBar.tsx"
      via: "import and render immediately after HeroSection"
      pattern: "TrustBar"
---

<objective>
Rebuild the hero section with a keyword H1, client-type subheadline, visible phone number, and a Get a Quote CTA. Add a trust bar strip directly below the hero with the four proof points.

Purpose: The current hero uses H2 and the CTA reads "View Our Work." This fails both SEO (no H1 keyword phrase) and conversion (wrong CTA above the fold). The rebuilt hero and trust bar directly address the first decision a prospect makes after finding the page.
Output: HeroSection.tsx, TrustBar.tsx, CSS additions, LandingPage.tsx updated to use both components.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
@D:/Projects/FaithandHarmony/src/pages/landing.css
@D:/Projects/FaithandHarmony/.planning/phases/03-above-fold-content/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection and TrustBar components</name>
  <files>src/components/landing/HeroSection.tsx, src/components/landing/TrustBar.tsx</files>
  <action>
    Create HeroSection.tsx.

    The component renders a section element with className "lp-hero" containing a div with className "lp-container".

    Inside the container:

    First child: an H1 element with className "lp-hero__headline". The text is two lines rendered as spans:
    - span with className "lp-hero__headline-main" and text "Drone Photography and Aerial Inspections"
    - span with className "lp-hero__headline-location" and text "Hampton Roads VA"

    Do NOT use the old H2 pattern. Phase 1 plan 01-04 moves H1 to a keyword phrase. This component implements that. The header logo text currently uses H1 in the original LandingPage.tsx. When this component is added, the logo H1 in the header should be changed to a span or div with the same styling in Task 2 of this plan.

    Second child: a p element with className "lp-hero__sub" and text:
    "Real estate agents and contractors across Hampton Roads rely on Sentinel for aerial photos, inspection data, and construction documentation. FAA Part 107 certified. Delivered in 48 hours or less."

    Write this copy following CLAUDE.md constraints. No dashes anywhere. Active voice. No marketing language. Two sentences, varied length.

    Third child: a div with className "lp-hero__actions" containing:
    - An anchor tag with href="#quote" className "lp-cta-button" and text "Get a Quote"
    - An anchor tag with href="tel:7605754876" className "lp-hero__phone" and text "760.575.4876"

    The hero section uses the existing lp-hero CSS class which already provides the background image and padding. Do not add a duplicate background declaration.

    ---

    Create TrustBar.tsx.

    The component renders a div element with className "lp-trust-bar" containing a div with className "lp-container".

    Inside the container, render a div with className "lp-trust-bar__badges" containing four badge items. Each badge is a div with className "lp-trust-bar__badge":
    - Badge 1: span with className "lp-trust-bar__label" text "FAA Part 107" and span with className "lp-trust-bar__detail" text "Certified"
    - Badge 2: span with className "lp-trust-bar__label" text "$1M Insurance" and span with className "lp-trust-bar__detail" text "Liability Coverage"
    - Badge 3: span with className "lp-trust-bar__label" text "Veteran Owned" and span with className "lp-trust-bar__detail" text "100% VOSB"
    - Badge 4: span with className "lp-trust-bar__label" text "48 Hour" and span with className "lp-trust-bar__detail" text "Turnaround"

    No emojis. No dashes. Use exact text above.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm HeroSection.tsx and TrustBar.tsx exist and compile without errors.</manual>
  </verify>
  <done>Both components exist, compile cleanly, and contain the correct text content and class names.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for hero rebuild and trust bar, update LandingPage</name>
  <files>src/pages/landing.css, src/pages/LandingPage.tsx</files>
  <action>
    In landing.css, append new styles after the sticky nav block added in plan 03-01. Do not change existing styles.

    Add hero sub-element styles:

    .landing-page .lp-hero__headline
    - margin-bottom: 24px

    .landing-page .lp-hero__headline-main
    - display: block
    - font-family: 'Saira Condensed', sans-serif
    - font-size: 72px
    - font-weight: 900
    - color: var(--warm-white)
    - letter-spacing: 4px
    - text-transform: uppercase
    - text-shadow: 0 0 40px rgba(255, 107, 53, 0.3)
    - line-height: 1.05

    .landing-page .lp-hero__headline-location
    - display: block
    - font-family: 'Saira Condensed', sans-serif
    - font-size: 48px
    - font-weight: 900
    - color: var(--safety-orange)
    - letter-spacing: 4px
    - text-transform: uppercase
    - margin-top: 8px

    .landing-page .lp-hero__sub
    - font-family: 'Share Tech Mono', monospace
    - font-size: 16px
    - color: var(--steel-gray)
    - max-width: 760px
    - margin: 0 auto 40px
    - line-height: 1.9
    - letter-spacing: 0.5px

    .landing-page .lp-hero__actions
    - display: flex
    - flex-direction: column
    - align-items: center
    - gap: 16px

    .landing-page .lp-hero__phone
    - font-family: 'Saira Condensed', sans-serif
    - font-size: 22px
    - font-weight: 700
    - letter-spacing: 2px
    - color: var(--warm-white)
    - text-decoration: none
    - transition: color 0.2s

    .landing-page .lp-hero__phone:hover
    - color: var(--safety-orange)

    Trust bar styles:

    .landing-page .lp-trust-bar
    - background: var(--gunmetal-light)
    - border-top: 1px solid rgba(255, 107, 53, 0.3)
    - border-bottom: 1px solid rgba(255, 107, 53, 0.3)
    - padding: 28px 0

    .landing-page .lp-trust-bar__badges
    - display: flex
    - justify-content: center
    - gap: 0
    - flex-wrap: wrap

    .landing-page .lp-trust-bar__badge
    - display: flex
    - flex-direction: column
    - align-items: center
    - padding: 16px 48px
    - border-right: 1px solid rgba(255, 107, 53, 0.2)

    .landing-page .lp-trust-bar__badge:last-child
    - border-right: none

    .landing-page .lp-trust-bar__label
    - font-family: 'Saira Condensed', sans-serif
    - font-size: 20px
    - font-weight: 900
    - color: var(--safety-orange)
    - letter-spacing: 2px
    - text-transform: uppercase

    .landing-page .lp-trust-bar__detail
    - font-family: 'Share Tech Mono', monospace
    - font-size: 11px
    - color: var(--steel-gray)
    - letter-spacing: 1.5px
    - text-transform: uppercase
    - margin-top: 4px

    In LandingPage.tsx:

    1. Import HeroSection: import HeroSection from '../components/landing/HeroSection'
    2. Import TrustBar: import TrustBar from '../components/landing/TrustBar'
    3. Replace the existing lp-hero section element (the JSX block starting with "section className='lp-hero'" and ending with its closing tag) with the HeroSection component: <HeroSection />
    4. Place TrustBar immediately after HeroSection and before the lp-services section: <TrustBar />
    5. In the header lp-logo-text div, the current H1 reads "SENTINEL AERIAL INSPECTIONS". Change this from an h1 to a div with the same className "lp-logo-text" content intact. The heading role of H1 now belongs to HeroSection. The logo text is a presentational label, not a document heading. Keep all existing CSS classes on the div.

    Do not change any other JSX.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
    <manual>Run npm run dev. Visit the landing page. Confirm the H1 keyword phrase appears above the fold. Confirm the phone number is visible and clickable. Confirm the CTA reads "Get a Quote." Confirm the trust bar appears below the hero with four badges. View source and confirm a single H1 element exists containing "Drone Photography and Aerial Inspections."</manual>
  </verify>
  <done>H1 contains the keyword phrase. Phone number is visible in hero. Primary CTA reads "Get a Quote" and links to #quote. Trust bar shows all four badges. No "View Our Work" CTA above the fold. TypeScript compiles and build succeeds.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors: npx tsc --noEmit
- Vite build succeeds: npm run build
- HeroSection.tsx exists at src/components/landing/HeroSection.tsx
- TrustBar.tsx exists at src/components/landing/TrustBar.tsx
- landing.css contains .lp-trust-bar and .lp-hero__headline styles
- LandingPage.tsx renders HeroSection before TrustBar before the services section
- Single H1 element in the rendered page DOM
</verification>

<success_criteria>
PAGE-02: Hero shows H1 keyword phrase, subheadline addressing realtors and contractors, phone number, and Get a Quote CTA above the fold.
PAGE-03: Trust bar shows FAA Part 107, $1M Insurance, Veteran Owned, and 48 Hour Turnaround badges.
CONV-01: Primary above-fold CTA reads "Get a Quote" not "View Our Work."
CONV-02: Phone number 760.575.4876 is visible in the hero section.
</success_criteria>

<output>
After completion, create `.planning/phases/03-above-fold-content/03-02-SUMMARY.md` using the summary template.
</output>
