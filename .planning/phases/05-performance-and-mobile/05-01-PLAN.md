---
phase: 05-performance-and-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/landing.css
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "When prefers-reduced-motion is active, the scanline overlay, scanline bar, and grid pulse produce no motion"
    - "When viewport is at or below 768px, the scanline overlay, scanline bar, and grid pulse produce no motion"
    - "The landing page CSS references only Saira Condensed and Share Tech Mono — no other font families appear in landing.css"
  artifacts:
    - path: "src/pages/landing.css"
      provides: "prefers-reduced-motion block disabling lp-flicker, lp-scanline, lp-gridPulse, lp-shimmer"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: "src/pages/landing.css"
      provides: "max-width 768px block that sets animation: none on the three permanent elements"
      contains: "animation: none"
  key_links:
    - from: "src/pages/landing.css .lp-scanline-overlay"
      to: "@media (prefers-reduced-motion: reduce)"
      via: "animation: none override"
      pattern: "lp-scanline-overlay.*animation"
    - from: "src/pages/landing.css .lp-scanline-bar"
      to: "@media (max-width: 768px)"
      via: "animation: none override"
      pattern: "lp-scanline-bar.*animation"
---

<objective>
Disable the three permanent CSS animations that run on every page render and drain battery on mobile. Verify the landing page CSS references exactly two font families.

Purpose: The scanline overlay (lp-flicker, 3s infinite), scanline bar (lp-scanline, 8s infinite), and grid pulse (lp-gridPulse, 4s infinite) run forever. On mobile they consume battery continuously. The veteran badge shimmer (lp-shimmer, 3s infinite) is also permanent and should be stopped in the same contexts. Entry animations (lp-fadeInUp, lp-slideInLeft, lp-slideInRight) fire once and are acceptable.

Output: Two new @media blocks appended to landing.css. No existing rules modified. PERF-02 verified as already satisfied (no change required).
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/.planning/STATE.md
@D:/Projects/FaithandHarmony/src/pages/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prefers-reduced-motion block to landing.css</name>
  <files>src/pages/landing.css</files>
  <action>
Append a new @media block to the end of landing.css (after the existing @media (max-width: 768px) block at line 796).

The block must cover prefers-reduced-motion: reduce. Inside it, target four selectors and set animation to none:

```css
@media (prefers-reduced-motion: reduce) {
  .landing-page .lp-scanline-overlay {
    animation: none;
  }
  .landing-page .lp-scanline-bar {
    animation: none;
  }
  .landing-page .lp-grid-bg {
    animation: none;
  }
  .landing-page .lp-veteran-badge::before {
    animation: none;
  }
}
```

Do NOT touch the @keyframes definitions. Do NOT touch the existing 768px block. Append only.

The entry animations (lp-fadeInUp on .lp-header, lp-slideInLeft on logo and h1, lp-slideInRight on .lp-veteran-badge, lp-fadeInUp on hero elements, lp-cta-button) are intentionally NOT disabled here. They are one-shot with backwards fill-mode and are acceptable under reduced motion guidelines as they do not loop.

If you want to go further and also disable entry animations in the reduced-motion block, you may add them, but it is not required for PERF-01.
  </action>
  <verify>
    <automated>grep -n "prefers-reduced-motion" /d/Projects/FaithandHarmony/src/pages/landing.css</automated>
    <manual>Open DevTools > Rendering tab > check "Emulate CSS media feature prefers-reduced-motion: reduce". Confirm the scanline bar is frozen and the overlay is static.</manual>
  </verify>
  <done>landing.css contains a @media (prefers-reduced-motion: reduce) block that sets animation: none on .lp-scanline-overlay, .lp-scanline-bar, .lp-grid-bg, and .lp-veteran-badge::before</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile animation disable and verify font scope</name>
  <files>src/pages/landing.css</files>
  <action>
Append a second block immediately after the prefers-reduced-motion block. This stops the permanent animations on actual mobile devices regardless of their accessibility settings.

```css
@media (max-width: 768px) {
  .landing-page .lp-scanline-overlay {
    animation: none;
  }
  .landing-page .lp-scanline-bar {
    animation: none;
  }
  .landing-page .lp-grid-bg {
    animation: none;
  }
  .landing-page .lp-veteran-badge::before {
    animation: none;
  }
}
```

Note: There is already a @media (max-width: 768px) block at line 796. Do NOT merge into that block. Append as a second 768px block. CSS cascade order means this second block wins and adds the animation overrides without disturbing the layout rules in the first block.

After appending, scan landing.css for font-family references. You should find only:
- 'Saira Condensed' (headings)
- 'Share Tech Mono' (body copy)

If any other font families appear in landing.css (such as 'JetBrains Mono' or 'Inter'), flag them. Based on current content, none should exist. Document your finding in the task output.

PERF-02 assessment: The Google Fonts link in index.html loads 4 families (JetBrains Mono, Inter, Saira Condensed, Share Tech Mono) because other app routes (admin, pilot portal) use JetBrains Mono and Inter. The landing page CSS itself only references 2 families. PERF-02 requires "Google Fonts reduced to 2 families" scoped to landing page usage. Since landing.css uses exactly 2, PERF-02 is satisfied without changing index.html. Document this conclusion in the summary.
  </action>
  <verify>
    <automated>grep -n "font-family" /d/Projects/FaithandHarmony/src/pages/landing.css | grep -v "Saira Condensed\|Share Tech Mono"</automated>
    <manual>Resize browser to 375px width. Confirm the horizontal scanline bar is not scrolling and the grid overlay is not pulsing.</manual>
  </verify>
  <done>A second @media (max-width: 768px) block exists at the end of landing.css with animation: none on all four permanent animation targets. No font families other than Saira Condensed and Share Tech Mono appear in landing.css. PERF-02 documented as satisfied.</done>
</task>

</tasks>

<verification>
Run: grep -c "prefers-reduced-motion" /d/Projects/FaithandHarmony/src/pages/landing.css
Expected output: 1 (exactly one reduced-motion block)

Run: grep -c "animation: none" /d/Projects/FaithandHarmony/src/pages/landing.css
Expected output: 8 or more (4 targets x 2 blocks)

Run: grep "font-family" /d/Projects/FaithandHarmony/src/pages/landing.css | grep -v "Saira\|Share"
Expected output: (empty — no other font families)
</verification>

<success_criteria>
- prefers-reduced-motion block exists in landing.css targeting all 4 permanent animation elements
- A second max-width: 768px block exists in landing.css with the same 4 animation: none overrides
- No font family other than Saira Condensed and Share Tech Mono appears in landing.css
- PERF-02 documented as satisfied (landing page CSS uses 2 families, index.html loads 4 for shared app use)
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-mobile/05-01-SUMMARY.md` using the summary template.
</output>
