---
phase: 05-performance-and-mobile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/landing.css
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02

must_haves:
  truths:
    - "At 900px viewport width, the layout is readable with appropriate font sizes and no horizontal overflow"
    - "At 375px viewport width, font sizes are reduced and the hero headline does not overflow its container"
    - "The .lp-container padding reduces on small mobile so content does not touch screen edges"
    - "Pricing cards stack vertically at 480px"
    - "Portfolio grid collapses to a single column at 480px"
  artifacts:
    - path: "src/pages/landing.css"
      provides: "Tablet breakpoint 768px to 1024px"
      contains: "@media (min-width: 769px) and (max-width: 1024px)"
    - path: "src/pages/landing.css"
      provides: "Small mobile breakpoint below 480px"
      contains: "@media (max-width: 480px)"
  key_links:
    - from: "src/pages/landing.css .lp-hero h2"
      to: "@media (max-width: 480px)"
      via: "font-size reduction to 36px"
      pattern: "lp-hero h2.*font-size"
    - from: "src/pages/landing.css .lp-container"
      to: "@media (max-width: 480px)"
      via: "padding reduction to 0 16px"
      pattern: "lp-container.*padding"
---

<objective>
Add two missing responsive breakpoints to landing.css. The current file has a single 768px breakpoint. This plan adds a tablet range (769px to 1024px) and a small mobile range (below 480px) to make the layout readable across all device sizes.

Purpose: Phase 3 and 4 build new sections (StickyNav, HeroSection, TrustBar, PricingSection, PortfolioGrid, MilitaryAirspace, FAQSection, QuoteForm, ServiceArea, AboutFounder). All of those sections are built using the existing .lp- CSS class conventions in landing.css. This plan adds the breakpoints those new sections will inherit. Writing the breakpoints now (before the sections exist) is correct because the breakpoints target classes already present in landing.css and classes the new components will follow by convention.

Output: Two new @media blocks appended to landing.css after the existing blocks from Plan 05-01.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/src/pages/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tablet breakpoint (769px to 1024px)</name>
  <files>src/pages/landing.css</files>
  <action>
Append a tablet media query block to the end of landing.css (after the blocks added in Plan 05-01).

This breakpoint handles the transition between mobile-single-column and full desktop layout. At tablet widths, grids should shift to 2-column where they currently use auto-fit, and font sizes should be between the desktop and mobile values.

```css
/* Tablet (769px to 1024px) */
@media (min-width: 769px) and (max-width: 1024px) {
  .landing-page .lp-container {
    padding: 0 24px;
  }
  .landing-page .lp-hero {
    padding: 80px 0;
  }
  .landing-page .lp-hero h2 {
    font-size: 56px;
  }
  .landing-page .lp-hero h2 span {
    font-size: 38px;
  }
  .landing-page .lp-section-title {
    font-size: 44px;
    margin-bottom: 50px;
  }
  .landing-page .lp-services-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  .landing-page .lp-service-card {
    padding: 32px;
  }
  .landing-page .lp-equipment-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  .landing-page .lp-equipment-item img {
    height: 300px;
  }
  .landing-page .lp-features-list {
    grid-template-columns: repeat(2, 1fr);
  }
  .landing-page .lp-vets-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  .landing-page .lp-contact-info {
    gap: 40px;
  }
}
```

Classes used by Phase 3/4 components that should also be covered (add these selectors to the same block). These will be defined by those phases but we establish the breakpoint rules now:

```css
  /* Phase 3/4 components — tablet rules */
  .landing-page .lp-pricing-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  .landing-page .lp-portfolio-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }
  .landing-page .lp-trust-bar {
    gap: 24px;
    flex-wrap: wrap;
    justify-content: center;
  }
  .landing-page .lp-military-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  .landing-page .lp-faq-grid {
    grid-template-columns: 1fr;
  }
```

All of these go inside the same (min-width: 769px) and (max-width: 1024px) block.
  </action>
  <verify>
    <automated>grep -c "min-width: 769px" /d/Projects/FaithandHarmony/src/pages/landing.css</automated>
    <manual>Open the landing page in browser. Set viewport to 900px. Verify services grid shows 2 columns, no horizontal overflow, hero text is readable.</manual>
  </verify>
  <done>landing.css contains a (min-width: 769px) and (max-width: 1024px) block with rules for container padding, hero sizing, grid layouts, and Phase 3/4 component classes</done>
</task>

<task type="auto">
  <name>Task 2: Add small mobile breakpoint (below 480px)</name>
  <files>src/pages/landing.css</files>
  <action>
Append a small mobile block after the tablet block. This handles phones at 375px to 480px where the existing 768px breakpoint leaves font sizes and padding still too large.

```css
/* Small mobile (below 480px) */
@media (max-width: 480px) {
  .landing-page .lp-container {
    padding: 0 16px;
  }
  .landing-page .lp-hero {
    padding: 60px 0;
  }
  .landing-page .lp-hero h2 {
    font-size: 36px;
    letter-spacing: 2px;
  }
  .landing-page .lp-hero h2 span {
    font-size: 26px;
  }
  .landing-page .lp-hero p {
    font-size: 12px;
  }
  .landing-page .lp-cta-button {
    font-size: 16px;
    padding: 16px 32px;
  }
  .landing-page .lp-section-title {
    font-size: 28px;
    margin-bottom: 40px;
    letter-spacing: 2px;
  }
  .landing-page .lp-service-card {
    padding: 24px;
  }
  .landing-page .lp-service-card h3 {
    font-size: 22px;
  }
  .landing-page .lp-logo-text h1 {
    font-size: 22px;
    letter-spacing: 2px;
  }
  .landing-page .lp-logo-section img {
    height: 48px;
  }
  .landing-page .lp-veteran-badge {
    font-size: 13px;
    padding: 8px 16px;
  }
  .landing-page .lp-features-list li {
    padding: 16px 16px 16px 40px;
  }
  .landing-page .lp-vets-content h2 {
    font-size: 28px;
    letter-spacing: 2px;
  }
  .landing-page .lp-vets-subtitle {
    font-size: 20px;
  }
  .landing-page .lp-about-content h3 {
    font-size: 26px;
  }
  .landing-page .lp-contact-item {
    min-width: unset;
    width: 100%;
  }

  /* Phase 3/4 components — small mobile rules */
  .landing-page .lp-pricing-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .landing-page .lp-pricing-card {
    padding: 24px;
  }
  .landing-page .lp-portfolio-grid {
    grid-template-columns: 1fr;
    gap: 12px;
  }
  .landing-page .lp-trust-bar {
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }
  .landing-page .lp-military-grid {
    grid-template-columns: 1fr;
  }
  .landing-page .lp-quote-form {
    padding: 24px 16px;
  }
}
```

These Phase 3/4 class names (.lp-pricing-grid, .lp-pricing-card, .lp-portfolio-grid, .lp-trust-bar, .lp-military-grid, .lp-quote-form) are the conventions those plans will follow. If Phase 3/4 use different class names, those plans update these selectors to match. Establishing them now means the breakpoint infrastructure is ready.
  </action>
  <verify>
    <automated>grep -c "max-width: 480px" /d/Projects/FaithandHarmony/src/pages/landing.css</automated>
    <manual>Set browser viewport to 375px. Verify hero h2 font-size is visibly smaller than at 768px, no text overflows its container, and CTA button is not clipped by clip-path at this width.</manual>
  </verify>
  <done>landing.css contains a (max-width: 480px) block with reduced font sizes, tighter padding, and single-column overrides for pricing and portfolio grids</done>
</task>

</tasks>

<verification>
Run: grep -n "@media" /d/Projects/FaithandHarmony/src/pages/landing.css
Expected: Four @media blocks total (original 768px, new prefers-reduced-motion from Plan 01, new 768px animation block from Plan 01, new 769px/1024px, new 480px)

Run: grep -c "grid-template-columns: 1fr" /d/Projects/FaithandHarmony/src/pages/landing.css
Expected: 4 or more (services, equipment, features, pricing, portfolio all collapse to 1fr on small mobile)
</verification>

<success_criteria>
- A (min-width: 769px) and (max-width: 1024px) block exists with 2-column grid rules and reduced padding
- A (max-width: 480px) block exists with reduced font sizes, 16px container padding, and 1-column grid overrides
- No existing rules were modified, only new blocks appended
- Phase 3/4 component class names (.lp-pricing-grid, .lp-portfolio-grid, .lp-trust-bar, .lp-military-grid, .lp-quote-form) are included in both new blocks as forward-declarations
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-mobile/05-02-SUMMARY.md` using the summary template.
</output>
