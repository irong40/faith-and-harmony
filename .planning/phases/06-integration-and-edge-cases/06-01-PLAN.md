---
phase: 06-integration-and-edge-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260303200000_vapi_call_logs_admin_rls.sql
  - src/pages/admin/CallLogs.tsx
  - src/components/admin/CallTranscriptDialog.tsx
  - src/pages/admin/Leads.tsx
  - src/pages/admin/components/AdminNav.tsx
  - src/App.tsx
autonomous: true
requirements: [INTG-03, INTG-04]
must_haves:
  truths:
    - "Admin can view a list of voice bot calls with timestamp, caller, duration, outcome, and transcript access"
    - "Admin can open a call transcript in a dialog with summary, duration badge, and linked quote request"
    - "Admin can view bot-sourced leads filtered by qualification status with conversion indicator"
    - "Admin can navigate to Call Logs and Leads pages from the main nav"
  artifacts:
    - path: "supabase/migrations/20260303200000_vapi_call_logs_admin_rls.sql"
      provides: "Admin read access RLS policy for vapi_call_logs"
      contains: "CREATE POLICY"
    - path: "src/pages/admin/CallLogs.tsx"
      provides: "Call log list page with outcome filter and transcript dialog trigger"
      min_lines: 80
    - path: "src/components/admin/CallTranscriptDialog.tsx"
      provides: "Transcript viewer dialog with summary and quote request link"
      min_lines: 30
    - path: "src/pages/admin/Leads.tsx"
      provides: "Bot-sourced leads table with pagination, search, qualification badges, conversion indicator"
      min_lines: 80
  key_links:
    - from: "src/pages/admin/CallLogs.tsx"
      to: "vapi_call_logs"
      via: "supabase .from('vapi_call_logs').select() with leads join"
      pattern: "from\\(['\"]vapi_call_logs['\"]\\)"
    - from: "src/pages/admin/Leads.tsx"
      to: "leads"
      via: "supabase .from('leads').select() with quote_requests join, filtered by source_channel voice_bot"
      pattern: "from\\(['\"]leads['\"]\\)"
    - from: "src/App.tsx"
      to: "src/pages/admin/CallLogs.tsx"
      via: "lazy import and Route element"
      pattern: "import.*CallLogs"
    - from: "src/App.tsx"
      to: "src/pages/admin/Leads.tsx"
      via: "lazy import and Route element"
      pattern: "import.*Leads"
    - from: "src/pages/admin/components/AdminNav.tsx"
      to: "/admin/call-logs"
      via: "navCategories items array"
      pattern: "call-logs"
---

<objective>
Build the two admin pages (Call Logs and Leads) that surface voice pipeline data to the admin, plus the RLS migration that enables admin reads on vapi_call_logs.

Purpose: Iron needs visibility into voice bot calls and bot-sourced leads. Without these pages, the voice pipeline data sits invisible in the database. The RLS migration unblocks admin queries against vapi_call_logs (currently restricted to service_role only).

Output: Two functional admin pages reachable from nav, one RLS migration applied, one transcript dialog component.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-and-edge-cases/06-RESEARCH.md
@.planning/phases/01-intake-api-and-lead-tracking/01-01-SUMMARY.md
@.planning/phases/01-intake-api-and-lead-tracking/01-03-SUMMARY.md

Key source files to reference for patterns:
@src/pages/admin/QuoteRequests.tsx (template for CallLogs table + Dialog pattern)
@src/pages/admin/Clients.tsx (template for Leads pagination + search pattern)
@src/pages/admin/components/AdminNav.tsx (nav structure to extend)
@src/App.tsx (lazy route registration)
@supabase/migrations/20260301191812_vapi_call_logs.sql (original schema, confirms no admin RLS)
@supabase/migrations/20260303100000_create_leads_table.sql (leads schema, has admin RLS already)
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS migration for vapi_call_logs admin read access</name>
  <files>supabase/migrations/20260303200000_vapi_call_logs_admin_rls.sql</files>
  <action>
    Create a new Supabase migration that adds an admin SELECT policy to vapi_call_logs. Follow the exact pattern from the leads table migration (20260303100000):

    ```sql
    CREATE POLICY "admins_read_vapi_call_logs" ON public.vapi_call_logs
      FOR SELECT TO authenticated
      USING (has_role(auth.uid(), 'admin'::app_role));
    ```

    Use `has_role(auth.uid(), 'admin'::app_role)` (NOT `profiles.role = 'admin'` which does not work in this project, per 01-01-SUMMARY.md decision).

    After creating the file, push the migration to remote Supabase with `npx supabase db push`. Verify it applied by checking `supabase migration list` shows the new migration as applied.

    Do NOT modify any existing migration files. This is a new migration only.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx supabase migration list 2>&1 | grep "vapi_call_logs_admin_rls"</automated>
    <manual>Confirm the migration appears in the list as applied (not pending)</manual>
  </verify>
  <done>Admin RLS policy exists on vapi_call_logs allowing authenticated admins to SELECT rows. Migration applied to remote Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: CallLogs page and CallTranscriptDialog component</name>
  <files>src/pages/admin/CallLogs.tsx, src/components/admin/CallTranscriptDialog.tsx</files>
  <action>
    Create two files following the patterns from QuoteRequests.tsx:

    **CallLogs.tsx** (follows QuoteRequests.tsx table + filter pattern):
    - Page header with Phone icon, "Call Logs" title, subtitle "Voice bot calls from the 757 line", Refresh button
    - Outcome filter bar: All, qualified, declined, transferred, voicemail, abandoned (Button toggle like QuoteRequests status filter)
    - Table columns: Time, Caller, Phone, Duration, Outcome, Request, Transcript
    - React Query fetch from `vapi_call_logs` with `leads ( caller_name, caller_phone, quote_request_id )` join
    - Order by `created_at` descending (fallback sort, since started_at can be null per Pitfall 3 in research)
    - Filter by outcome when not "All"
    - Limit 100 rows
    - Time column: `format(new Date(log.started_at ?? log.created_at), "MMM d, h:mm a")` using date-fns. Handle null started_at by falling back to created_at.
    - Caller column: `log.leads?.caller_name ?? log.caller_number ?? "Unknown"`
    - Duration column: formatted as "Xm Ys" (e.g., "2m 34s") or just "Xs" for under a minute
    - Outcome column: Badge with color map (qualified=green, declined=red, transferred=blue, voicemail=slate, abandoned=gray, pending=amber). Show "Unknown" badge in neutral gray for null outcome.
    - Request column: "Linked" green badge if `leads?.quote_request_id` exists, "None" muted text otherwise
    - Transcript column: "View" ghost button with FileText icon if transcript exists, "No transcript" muted text otherwise. Clicking opens CallTranscriptDialog.
    - Loading state: centered "Loading call logs..." text
    - Empty state: centered "No calls found." text
    - Import AdminNav and render it at the top of the page

    **CallTranscriptDialog.tsx**:
    - Props: `callLog: CallLogRow | null` and `onClose: () => void`
    - Dialog opens when callLog is truthy
    - DialogTitle: "Call: {caller_name or caller_number or 'Unknown'}"
    - Metadata row: duration formatted as "Xm Ys", outcome Badge, "View Quote Request" link to `/admin/quote-requests` if quote_request_id exists
    - Summary section: muted background div showing `callLog.summary` if present
    - Transcript section: ScrollArea with pre tag, `whitespace-pre-wrap font-mono`, showing full transcript or "No transcript available."
    - Recording link: if `callLog.recording_url` is truthy, show a small external link icon button labeled "Recording" that opens the URL in a new tab. Do not build a media player.
    - Max width 2xl, max height 85vh with flex column layout

    Type definition for CallLogRow:
    ```typescript
    type CallLogRow = {
      id: string;
      call_id: string;
      caller_number: string | null;
      duration_seconds: number;
      started_at: string | null;
      ended_at: string | null;
      created_at: string;
      outcome: string | null;
      transcript: string | null;
      summary: string | null;
      recording_url: string | null;
      lead_id: string | null;
      leads: {
        caller_name: string;
        caller_phone: string;
        quote_request_id: string | null;
      } | null;
    };
    ```

    Use OUTCOME_COLORS map matching the research pattern. Export it from CallLogs.tsx so Leads.tsx can import it (or define a shared constant).

    Do NOT fetch transcripts from the Vapi API. Query the database only.
    Do NOT build a recording media player. Simple external link if recording_url exists.
    Do NOT create a separate /admin/call-logs/:id detail route. Use the Dialog pattern.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify CallLogs.tsx and CallTranscriptDialog.tsx compile without type errors</manual>
  </verify>
  <done>CallLogs.tsx renders a filterable table of voice bot calls with outcome badges, duration formatting, and transcript dialog. CallTranscriptDialog.tsx shows full transcript in a scrollable dialog with summary and metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Leads page, route registration, and nav wiring</name>
  <files>src/pages/admin/Leads.tsx, src/App.tsx, src/pages/admin/components/AdminNav.tsx</files>
  <action>
    Create the Leads page and wire both new pages into the app routing and navigation.

    **Leads.tsx** (follows Clients.tsx pagination + search pattern):
    - Page header with Target icon, "Leads" title, subtitle "Voice bot sourced leads and conversion tracking", Refresh button
    - Qualification status filter bar: All, qualified, declined, transferred, pending (Button toggle)
    - Search input for caller name or phone (using Input component like Clients.tsx)
    - Table columns: Date, Caller, Phone, Email, Status, Converted, Actions
    - React Query fetch from `leads` with `quote_requests ( id, status )` join
    - CRITICAL: Filter by `source_channel = 'voice_bot'` per INTG-04 requirement. Do not show web or manual leads.
    - Order by `created_at` descending
    - Paginated with PAGE_SIZE = 20, using `.range()` and `{ count: 'exact' }` like Clients.tsx
    - Search filters on `caller_name.ilike` and `caller_phone.ilike` using `.or()`
    - Status filter on `qualification_status` when not "All"
    - Date column: `format(new Date(lead.created_at), "MMM d, h:mm a")` using date-fns
    - Caller column: `lead.caller_name`
    - Phone column: `lead.caller_phone`
    - Email column: `lead.caller_email ?? "No email"`
    - Status column: Badge with same OUTCOME_COLORS map (qualified=green, declined=red, transferred=blue, pending=amber)
    - Converted column: If `lead.quote_requests` exists, show green "Quoted" badge with the quote status in parentheses. If `lead.quote_request_id` is not null but join returns null, show "Linked" neutral badge. If `quote_request_id` is null, show "No quote" in muted text. This is correct behavior for declined leads (per Pitfall 2 in research).
    - Pagination: Previous/Next buttons with page count display, matching Clients.tsx pattern
    - Loading state: centered "Loading leads..." text
    - Empty state: centered "No leads found." text
    - Import AdminNav and render it at the top of the page
    - staleTime: 2 minutes (2 * 60 * 1000)

    Type definition for LeadRow:
    ```typescript
    type LeadRow = {
      id: string;
      created_at: string;
      caller_name: string;
      caller_phone: string;
      caller_email: string | null;
      source_channel: string;
      qualification_status: string;
      call_id: string | null;
      client_id: string | null;
      quote_request_id: string | null;
      quote_requests: {
        id: string;
        status: string;
      } | null;
    };
    ```

    **App.tsx changes:**
    - Add two lazy imports after the existing QuoteRequests import:
      ```
      const CallLogs = lazy(() => import("./pages/admin/CallLogs"));
      const AdminLeads = lazy(() => import("./pages/admin/Leads"));
      ```
      NOTE: Name the Leads import `AdminLeads` to avoid potential conflict with any existing Leads export. If no conflict exists, `Leads` is fine, but check first.
    - Add two Route elements after the quote-requests Route, before the pilot portal routes:
      ```
      <Route path="/admin/call-logs" element={<AdminRoute><CallLogs /></AdminRoute>} />
      <Route path="/admin/leads" element={<AdminRoute><AdminLeads /></AdminRoute>} />
      ```

    **AdminNav.tsx changes:**
    - Add `Phone` to the lucide-react import (Target is already imported)
    - Add two items to the existing "Quotes" category items array:
      ```
      { href: "/admin/call-logs", label: "Call Logs", icon: Phone },
      { href: "/admin/leads", label: "Leads", icon: Target },
      ```
    - Place them after the existing "Quote Requests" item

    Do NOT create a new nav category. Extend the existing "Quotes" category.
    Do NOT show leads from source_channel other than 'voice_bot'.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify all three files compile, routes registered, nav items visible</manual>
  </verify>
  <done>Leads.tsx renders a paginated, searchable table of voice bot leads with qualification badges and conversion indicators. Both CallLogs and Leads pages are reachable via /admin/call-logs and /admin/leads routes. AdminNav shows both pages under the Quotes category.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes without errors (Vite build)
3. Admin navigating to /admin/call-logs sees the call log table (may be empty if no calls exist yet, but page renders without errors)
4. Admin navigating to /admin/leads sees the leads table filtered to voice_bot source (may be empty)
5. Both pages appear in the Quotes dropdown in AdminNav
6. Clicking a transcript icon opens the CallTranscriptDialog with call details
</verification>

<success_criteria>
- vapi_call_logs has admin read RLS policy applied to remote Supabase
- CallLogs page queries vapi_call_logs with leads join, displays filterable table, opens transcript dialog
- Leads page queries leads with quote_requests join, filters by voice_bot source, shows pagination and conversion indicators
- Both pages registered as lazy routes in App.tsx behind AdminRoute guard
- Both pages linked in AdminNav under Quotes category
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-and-edge-cases/06-01-SUMMARY.md`
</output>
