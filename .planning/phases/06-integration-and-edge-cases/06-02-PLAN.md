---
phase: 06-integration-and-edge-cases
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [INTG-01, INTG-02]
must_haves:
  truths:
    - "A call through the full pipeline results in a request visible in admin within 60 seconds"
    - "An out-of-area caller receives a polite decline and no quote request is created"
    - "A complex job caller is offered a callback or transfer to Iron"
    - "Edge case calls produce correct outcome and qualification_status values in the database"
  artifacts: []
  key_links:
    - from: "Vapi 757 number"
      to: "n8n webhook"
      via: "end-of-call webhook"
      pattern: "vapi_call_logs row created"
    - from: "n8n workflow"
      to: "intake-lead edge function"
      via: "HTTP POST with webhook secret"
      pattern: "quote_requests row created"
    - from: "vapi_call_logs.outcome"
      to: "leads.qualification_status"
      via: "intake-lead function sets both"
      pattern: "qualified|declined|transferred"
---

<objective>
Validate the end-to-end voice pipeline and edge case routing by running test calls through the live system.

Purpose: The entire v1.1 pipeline (Phases 1 through 5) culminates in a working call-to-invoice flow. This plan validates that the pipeline works as a unit and that edge cases (out of area, complex jobs, payment questions) produce the correct database state. Without this validation, the pipeline ships untested.

Output: Confirmed working pipeline with documented test results. Any gaps identified and fixed.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-and-edge-cases/06-RESEARCH.md
@.planning/phases/06-integration-and-edge-cases/06-01-SUMMARY.md
@.planning/phases/01-intake-api-and-lead-tracking/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-validation system check and edge case audit</name>
  <files></files>
  <action>
    Before running live test calls, verify the pipeline is ready by checking each link in the chain:

    1. **Vapi assistant exists and is active:** Check that the Phase 2 system prompt file exists in the repo and contains all 6 service packages with pricing. Verify it includes Hampton Roads service area boundaries and edge case routing instructions (out of area, complex jobs over $1200, payment questions).

    2. **n8n workflow is deployed:** Verify the Phase 3 n8n workflow JSON exists in the repo. Check that it references the correct Vapi webhook endpoint and calls the intake-lead edge function.

    3. **intake-lead edge function is live:** Run a curl test against the deployed edge function with a test payload to confirm it responds (use the same pattern from 01-03-SUMMARY.md verification). Use the INTAKE_WEBHOOK_SECRET from the Supabase secrets.

    4. **Admin pages render:** Run `npm run build` to confirm the Plan 06-01 pages compile. Start the dev server if not running and verify /admin/call-logs and /admin/leads load without errors (even if empty).

    5. **Edge case routing audit:** Read the Phase 2 system prompt and verify these scenarios are covered:
       - Out of service area: Bot should decline politely and suggest searching for local providers
       - Complex/commercial jobs over $1200: Bot should offer to transfer to Iron or take a callback number
       - Payment questions: Bot should redirect to Iron
       - Existing client follow-ups: Bot should offer callback

    If any of these are missing from the system prompt, document what needs to be added. Do NOT modify the system prompt in this plan (that is a Phase 2 artifact). Instead, note it as a gap in the summary.

    6. **Database state check:** Query vapi_call_logs and leads tables to confirm both have the expected columns (outcome, lead_id, qualification_status, source_channel) and that the admin RLS policy from Plan 06-01 allows reads.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npm run build 2>&1 | tail -5</automated>
    <manual>All 6 pre-validation checks pass. Edge case routing exists in system prompt. intake-lead responds to test curl.</manual>
  </verify>
  <done>Full pipeline chain verified from Vapi config through n8n workflow through intake-lead through database. Edge case scenarios confirmed in system prompt or documented as gaps. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end pipeline test and edge case verification</name>
  <files></files>
  <action>Prepare the test checklist and document expected database state for each test scenario. After the human runs the test calls, verify the database rows match expected outcomes.</action>
  <what-built>
    The complete voice bot pipeline: caller dials 757 number, talks to Vapi bot, call ends, Vapi fires webhook to n8n, n8n calls intake-lead edge function, edge function creates client + quote_request + lead rows, admin pages display the data.

    Task 1 verified all pipeline components are in place. This checkpoint validates the live end-to-end flow with real calls.
  </what-built>
  <how-to-verify>
    **Test 1: Happy path (INTG-01)**
    1. Call the 757 number
    2. When the bot answers, provide: your name, a Hampton Roads address, service type "Listing Pro", preferred date next week
    3. Let the bot complete the call naturally
    4. Start a timer when you hang up
    5. Within 60 seconds, go to /admin/quote-requests and click Refresh
    6. Verify the new request appears with source "voice_bot" and status "new"
    7. Go to /admin/call-logs and verify the call appears with outcome "qualified" and a transcript
    8. Click the transcript icon and verify the dialog shows the call summary and full transcript
    9. Go to /admin/leads and verify the lead appears with qualification_status "qualified" and a "Quoted" badge in the Converted column

    **Test 2: Out of area decline (INTG-02)**
    1. Call the 757 number
    2. When asked about location, say "I am in Richmond, Virginia" or "I am in Raleigh, North Carolina" (outside Hampton Roads service area)
    3. Verify the bot responds with a polite decline and suggests searching for local drone providers
    4. After hanging up, check /admin/call-logs for outcome "declined"
    5. Check /admin/leads for qualification_status "declined" and quote_request_id null (no quote column shows "No quote")

    **Test 3: Complex job transfer (INTG-02)**
    1. Call the 757 number
    2. Describe a large commercial inspection project ("I need a full building inspection for a 20 story office tower, budget is around $3000")
    3. Verify the bot offers to transfer to Iron or take a callback number
    4. After hanging up, check /admin/call-logs for outcome "transferred"

    If any test fails, describe what happened and what the expected behavior should have been.
  </how-to-verify>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && echo "Checkpoint: requires manual test calls through live 757 number"</automated>
  </verify>
  <done>At least one happy-path call creates a visible quote request within 60 seconds. Edge case calls produce correct outcome values in the database. All test results documented.</done>
  <resume-signal>Type "approved" if all 3 tests pass. If any test fails, describe the failure and whether it needs a system prompt fix (Phase 2 artifact) or a pipeline fix.</resume-signal>
</task>

</tasks>

<verification>
1. Happy path call creates a visible quote request within 60 seconds of hangup
2. Out-of-area call results in polite decline with declined outcome in database
3. Complex job call results in transfer offer with transferred outcome in database
4. All call data visible on /admin/call-logs with correct outcome badges
5. All lead data visible on /admin/leads with correct qualification and conversion indicators
</verification>

<success_criteria>
- At least one successful end-to-end call creates a quote_request visible in admin within 60 seconds (INTG-01)
- Out-of-area and complex job scenarios produce correct outcome and qualification_status values (INTG-02)
- All test calls appear on the Call Logs and Leads admin pages with correct data
- Any pipeline gaps are documented with specific remediation steps
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-and-edge-cases/06-02-SUMMARY.md`
</output>
