---
phase: 07-foundation-and-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/admin/components/QuoteBuilder.tsx
  - supabase/functions/create-deposit-invoice/index.ts
  - public/pwa-192x192.png
  - public/pwa-512x512.png
autonomous: false
requirements: [BILL-01, DEPLOY-01]

must_haves:
  truths:
    - "Quote creation suggests deposit at 50% of package price, not 25%"
    - "Square deposit invoice description says 50%, not 25%"
    - "Square deposit invoice line item says Deposit (50%), not Deposit (25%)"
    - "PWA install prompt shows Sentinel branded icon, not SVG placeholder"
  artifacts:
    - path: "src/pages/admin/components/QuoteBuilder.tsx"
      provides: "50% deposit calculation and UI text"
      contains: "0.5"
    - path: "supabase/functions/create-deposit-invoice/index.ts"
      provides: "50% deposit description in Square invoice"
      contains: "50%"
    - path: "public/pwa-192x192.png"
      provides: "Sentinel branded 192x192 PWA icon"
    - path: "public/pwa-512x512.png"
      provides: "Sentinel branded 512x512 PWA icon"
  key_links:
    - from: "src/pages/admin/components/QuoteBuilder.tsx"
      to: "supabase/functions/create-deposit-invoice/index.ts"
      via: "deposit_amount flows through quotes table"
      pattern: "deposit_amount"
---

<objective>
Fix the deposit percentage from 25% to 50% across the quote builder UI and Square invoice edge function, and replace placeholder PWA icons with Sentinel branded PNGs.

Purpose: Ensures quotes charge the correct 50% deposit per business requirements, and the Trestle PWA looks professional when installed on mobile devices.
Output: Corrected deposit calculations/text in 2 files, branded PWA icon files.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-and-quick-wins/07-RESEARCH.md

<interfaces>
<!-- Key locations for deposit fix. Extracted from codebase. -->

From src/pages/admin/components/QuoteBuilder.tsx:
```typescript
// Line 51: suggestedDeposit calculation
const suggestedDeposit = Math.round(total * 0.25 * 100) / 100;
// Change 0.25 to 0.5

// Line 228: placeholder text
placeholder={`Suggested 25%: $${suggestedDeposit.toFixed(2)}`}
// Change "25%" to "50%"

// Line 233: suggestion text
Suggested 25% deposit: ${suggestedDeposit.toFixed(2)}
// Change "25%" to "50%"
```

From supabase/functions/create-deposit-invoice/index.ts:
```typescript
// Line 148: invoice title description
description: `25% deposit for ${requestInfo.job_type ?? "aerial services"} -- Job ${shortId}`,
// Change "25%" to "50%"

// Line 151: line item name
name: `Deposit (25%) -- ${requestInfo.job_type ?? "Aerial Services"}`,
// Change "25%" to "50%"
```

PWA manifest config (vite.config.ts, no changes needed):
```typescript
// Icons reference public/pwa-192x192.png and public/pwa-512x512.png
// Same filenames, just replace file contents
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix deposit percentage from 25% to 50%</name>
  <files>
    src/pages/admin/components/QuoteBuilder.tsx,
    supabase/functions/create-deposit-invoice/index.ts
  </files>
  <behavior>
    - suggestedDeposit calculates as total * 0.5 (not 0.25)
    - QuoteBuilder placeholder text says "Suggested 50%" not "Suggested 25%"
    - QuoteBuilder suggestion text says "50% deposit" not "25% deposit"
    - Edge function invoice description says "50% deposit" not "25% deposit"
    - Edge function line item name says "Deposit (50%)" not "Deposit (25%)"
    - No other instances of "25%" related to deposits exist in the codebase
  </behavior>
  <action>
    1. In src/pages/admin/components/QuoteBuilder.tsx:
       - Line 51: Change `total * 0.25` to `total * 0.5`
       - Line 228: Change `Suggested 25%` to `Suggested 50%`
       - Line 233: Change `Suggested 25% deposit` to `Suggested 50% deposit`

    2. In supabase/functions/create-deposit-invoice/index.ts:
       - Line 148: Change `25% deposit for` to `50% deposit for`
       - Line 151: Change `Deposit (25%)` to `Deposit (50%)`

    3. After making changes, grep the entire codebase for any remaining "25%" references related to deposits to confirm nothing was missed. Run: `grep -rn "25%" src/ supabase/functions/create-deposit-invoice/` and verify no deposit-related hits remain.

    4. Deploy the updated edge function: `supabase functions deploy create-deposit-invoice --use-api` (per project convention, always use --use-api flag on this machine).
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All deposit references say 50% instead of 25%. suggestedDeposit calculates at half the total. No remaining 25% deposit strings in QuoteBuilder or create-deposit-invoice.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace PWA icons with Sentinel branded PNGs</name>
  <files>
    public/pwa-192x192.png,
    public/pwa-512x512.png
  </files>
  <action>
    Generate Sentinel branded PWA icons using the project brand colors (F&H purple #5B2C6F and gold #C9A227).

    1. Create a 512x512 PNG icon with:
       - Solid purple (#5B2C6F) background
       - Gold (#C9A227) "S" letter centered (for Sentinel), large and bold
       - Safe zone padding: logo content within the inner 80% circle (required for maskable icon)
       - Clean, professional look suitable for a home screen icon

    2. Create a 192x192 PNG that is a scaled-down version of the 512x512 icon.

    3. Save both files overwriting the existing placeholders at:
       - public/pwa-192x192.png
       - public/pwa-512x512.png

    Use Canvas API via a Node.js script with the `canvas` package (npm install canvas temporarily), or use ImageMagick/sharp if available. The icon design should be simple and readable at small sizes.

    If neither canvas nor ImageMagick is available on this machine, create the icons using an alternative approach (SVG rendered to PNG, or base64 encoded PNG written directly). The key requirement is two properly sized PNG files with Sentinel branding, not SVG placeholders.

    After generating, verify file sizes are reasonable (each should be several KB, not the tiny 2KB/9KB placeholders).
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && node -e "const fs=require('fs'); const a=fs.statSync('public/pwa-192x192.png').size; const b=fs.statSync('public/pwa-512x512.png').size; console.log('192:', a, 'bytes'); console.log('512:', b, 'bytes'); if(a<1000||b<1000) process.exit(1); console.log('OK: both icons exist with reasonable size')"</automated>
  </verify>
  <done>
    public/pwa-192x192.png and public/pwa-512x512.png contain Sentinel branded icons with purple background and gold "S" at correct dimensions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deposit fix and PWA icons</name>
  <action>Human verification of visual and functional changes that cannot be automated.</action>
  <what-built>
    Deposit percentage fixed from 25% to 50% in QuoteBuilder and Square invoice edge function. PWA icons replaced with Sentinel branded PNGs (purple background, gold S).
  </what-built>
  <how-to-verify>
    1. Open /admin/quote-requests, select any request, open QuoteBuilder
    2. Add a line item (e.g., $450), verify the suggested deposit shows 50% ($225), not 25%
    3. Verify placeholder and suggestion text both say "50%"
    4. For PWA icons: run `npm run build` and check `dist/pwa-192x192.png` and `dist/pwa-512x512.png` visually
    5. If possible, install the PWA on a mobile device and verify the home screen icon shows Sentinel branding
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Open QuoteBuilder and verify 50% deposit suggestion
2. Grep codebase for remaining "25%" deposit references (should find none)
3. Visually inspect PWA icons for Sentinel branding
4. `npx vitest run` passes
5. Edge function deployed successfully
</verification>

<success_criteria>
- QuoteBuilder calculates suggestedDeposit as 50% of total
- All UI text says "50%" not "25%"
- Square invoice description and line item say "50%"
- PWA icons are Sentinel branded PNGs at 192x192 and 512x512
- No remaining "25% deposit" strings in codebase
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-and-quick-wins/07-02-SUMMARY.md`
</output>
