---
phase: 08-watermark-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260305600000_watermark_previews_bucket.sql
  - supabase/migrations/20260305600100_drone_jobs_private_bucket.sql
  - supabase/migrations/20260305600200_preview_urls_column.sql
  - src/components/drone/QADetailModal.tsx
autonomous: true
requirements: [BILL-03]

must_haves:
  truths:
    - "Watermarked preview thumbnails bucket exists and is publicly readable"
    - "Original drone-jobs bucket is private and requires signed URL access"
    - "drone_jobs table has a preview_urls column to store watermarked preview URLs"
    - "Admin UI still displays asset previews after drone-jobs bucket becomes private"
  artifacts:
    - path: "supabase/migrations/20260305600000_watermark_previews_bucket.sql"
      provides: "Public watermark-previews storage bucket"
      contains: "watermark-previews"
    - path: "supabase/migrations/20260305600100_drone_jobs_private_bucket.sql"
      provides: "Reverts drone-jobs to private bucket"
      contains: "public = false"
    - path: "supabase/migrations/20260305600200_preview_urls_column.sql"
      provides: "preview_urls TEXT[] column on drone_jobs"
      contains: "preview_urls"
    - path: "src/components/drone/QADetailModal.tsx"
      provides: "Fixed asset URL generation using signed URLs instead of public URLs"
  key_links:
    - from: "supabase/migrations/20260305600100_drone_jobs_private_bucket.sql"
      to: "src/components/drone/QADetailModal.tsx"
      via: "Private bucket requires signed URL in admin UI"
      pattern: "createSignedUrl"
---

<objective>
Create the Supabase storage infrastructure for the watermark pipeline. A new public `watermark-previews` bucket stores preview thumbnails. The existing `drone-jobs` bucket reverts to private so originals require signed URL access. A `preview_urls` column on `drone_jobs` stores public preview URLs for the balance due email. The admin QA modal switches from public URL parsing to signed URL access.

Purpose: Establishes the storage separation that prevents clients from accessing full resolution originals before payment while keeping watermarked previews publicly accessible for email embedding.

Output: Three Supabase migrations and one React component fix.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-watermark-pipeline/08-RESEARCH.md

<interfaces>
<!-- Existing migration patterns for storage buckets -->

From supabase/migrations/20260125120000_drone_processed_bucket.sql:
```sql
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES ('drone-processed-assets', 'drone-processed-assets', true, null, null)
ON CONFLICT (id) DO NOTHING;

CREATE POLICY "Public Access" ON storage.objects FOR SELECT
USING (bucket_id = 'drone-processed-assets');
```

From supabase/migrations/20260125140000_drone_jobs_public_bucket.sql:
```sql
UPDATE storage.buckets SET public = true WHERE id = 'drone-jobs';
DROP POLICY IF EXISTS "drone_jobs_public_read" ON storage.objects;
CREATE POLICY "drone_jobs_public_read" ON storage.objects FOR SELECT TO public
USING (bucket_id = 'drone-jobs');
```

From src/components/drone/QADetailModal.tsx line 84:
```typescript
const storagePath = asset.file_path.split('/storage/v1/object/public/drone-jobs/')[1];
if (storagePath) {
  await supabase.storage.from('drone-jobs').remove([storagePath]);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create storage bucket migrations</name>
  <files>supabase/migrations/20260305600000_watermark_previews_bucket.sql, supabase/migrations/20260305600100_drone_jobs_private_bucket.sql, supabase/migrations/20260305600200_preview_urls_column.sql</files>
  <action>
Create three sequential Supabase migrations.

Migration 1 (20260305600000_watermark_previews_bucket.sql): Create the `watermark-previews` public bucket. Insert into storage.buckets with id='watermark-previews', name='watermark-previews', public=true, file_size_limit=5242880 (5 MB), allowed_mime_types=ARRAY['image/jpeg','image/png']. Use ON CONFLICT (id) DO NOTHING. Create a SELECT policy "Public read watermark previews" on storage.objects USING (bucket_id = 'watermark-previews'). No upload policy needed because n8n uses the service role key which bypasses RLS.

Migration 2 (20260305600100_drone_jobs_private_bucket.sql): Revert the drone-jobs bucket to private. UPDATE storage.buckets SET public = false WHERE id = 'drone-jobs'. DROP POLICY IF EXISTS "drone_jobs_public_read" ON storage.objects. Also drop the old "Public read access" policy from storage.policies if it exists (DROP POLICY IF EXISTS "Public read access" ON storage.objects). Keep the authenticated upload, update, and delete policies intact. Add a comment explaining that service role key bypasses RLS so admin UI and n8n continue working. The anon key will no longer grant read access, which is the point.

Migration 3 (20260305600200_preview_urls_column.sql): Add preview_urls TEXT[] DEFAULT '{}' to public.drone_jobs. Add a COMMENT ON COLUMN explaining these are public URLs of watermarked preview thumbnails in the watermark-previews bucket, populated by the n8n pipeline watermark step.

Before creating, verify no timestamp conflicts by checking the latest migration timestamp in supabase/migrations/. Use timestamps that come after the last existing migration.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && ls supabase/migrations/*watermark* supabase/migrations/*private_bucket* supabase/migrations/*preview_urls* 2>/dev/null | wc -l</automated>
  </verify>
  <done>Three migration files exist. watermark-previews bucket is public. drone-jobs bucket is set to private. preview_urls column is added to drone_jobs. No existing policies are broken for authenticated or service role access.</done>
</task>

<task type="auto">
  <name>Task 2: Fix QADetailModal for private drone-jobs bucket</name>
  <files>src/components/drone/QADetailModal.tsx</files>
  <action>
The QADetailModal currently parses public URLs to extract storage paths (line 84: `asset.file_path.split('/storage/v1/object/public/drone-jobs/')[1]`). After reverting drone-jobs to private, the `file_path` stored in `drone_assets` may still contain public URL patterns but public access will return 403.

Search the entire `src/` directory for any other references to `/storage/v1/object/public/drone-jobs/` and fix all occurrences. The grep from research found only QADetailModal.tsx.

In QADetailModal.tsx, the delete handler on line 84 extracts the storage path from `asset.file_path`. The path extraction logic itself is fine for the delete operation (it uses `supabase.storage.from('drone-jobs').remove()` which uses the authenticated client). However, if the component also displays images using the public URL pattern, those will break.

Check the full component for any `<img>` or image display that constructs a public URL for drone-jobs. If found, replace with a signed URL approach using `supabase.storage.from('drone-jobs').createSignedUrl(path, 3600)` (1 hour expiry). The delete path extraction can remain as is since `.remove()` uses the storage path, not a public URL.

Also check for the URL parsing approach. If `asset.file_path` stores the full public URL, extract the path portion after the bucket name. The storage client `.remove()` and `.createSignedUrl()` methods take the path relative to the bucket root (e.g., `{job_id}/raw/photo.jpg`), not the full URL.

Do NOT change the delete logic unless the path extraction is incorrect for the new private bucket. The `.remove()` method works the same regardless of bucket privacy. Focus on any image display that relies on public URL access.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && grep -rn "storage/v1/object/public/drone-jobs" src/ | wc -l</automated>
  </verify>
  <done>Zero references to public drone-jobs URLs remain in the React codebase. Any image display in QADetailModal uses signed URLs or the Supabase client getPublicUrl/createSignedUrl methods. The delete handler continues to work with the private bucket.</done>
</task>

</tasks>

<verification>
- All three migration files exist with correct SQL syntax
- No references to `/storage/v1/object/public/drone-jobs/` remain in src/
- `npx vitest run --reporter=verbose` passes (existing tests not broken)
</verification>

<success_criteria>
- watermark-previews bucket migration creates a public bucket with SELECT policy
- drone-jobs bucket migration reverts to private and drops public read policies
- preview_urls TEXT[] column migration adds the column to drone_jobs
- QADetailModal works with private drone-jobs bucket (no public URL dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/08-watermark-pipeline/08-01-SUMMARY.md`
</output>
