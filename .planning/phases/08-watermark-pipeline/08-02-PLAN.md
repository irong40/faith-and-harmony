---
phase: 08-watermark-pipeline
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - assets/watermark-tile.png
  - n8n-workflows/wf1-sentinel-pipeline-orchestrator.json
autonomous: false
requirements: [BILL-03]

must_haves:
  truths:
    - "After job processing completes, 2 to 3 watermarked preview thumbnails exist in the watermark-previews bucket"
    - "Watermarked preview URLs are accessible without authentication"
    - "A tiled watermark overlay is visible on each preview and cannot be cropped out to recover the original"
    - "Preview URLs are stored in the drone_jobs.preview_urls column"
  artifacts:
    - path: "assets/watermark-tile.png"
      provides: "Tiled diagonal watermark overlay PNG with SENTINEL AERIAL text"
    - path: "n8n-workflows/wf1-sentinel-pipeline-orchestrator.json"
      provides: "Updated pipeline with watermark_preview step between qa_gate and packaging"
  key_links:
    - from: "n8n-workflows/wf1-sentinel-pipeline-orchestrator.json"
      to: "watermark-previews bucket"
      via: "HTTP Request node uploads watermarked JPEGs to Supabase Storage"
      pattern: "watermark-previews"
    - from: "n8n-workflows/wf1-sentinel-pipeline-orchestrator.json"
      to: "drone_jobs.preview_urls"
      via: "HTTP Request PATCH to update preview_urls array after upload"
      pattern: "preview_urls"
    - from: "assets/watermark-tile.png"
      to: "n8n Execute Command node"
      via: "Watermark tile deployed to processing rig at /opt/sentinel/assets/watermark-tile.png"
      pattern: "/opt/sentinel/assets/watermark-tile.png"
---

<objective>
Build the n8n pipeline watermark step and watermark asset. A tiled diagonal watermark PNG is created and stored in the repo. The n8n WF1 pipeline gets a new watermark_preview step inserted after qa_gate that selects the top 2 to 3 images by QA score, resizes them to 1200px wide previews, composites the tiled watermark overlay, strips EXIF metadata, uploads to the watermark-previews bucket, and writes the public URLs to drone_jobs.preview_urls.

Purpose: Generates the watermarked previews that Phase 9 (Billing Lifecycle) needs for the balance due email. Clients see proof of work quality without accessing full resolution originals.

Output: Watermark PNG asset and updated n8n pipeline workflow JSON.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-watermark-pipeline/08-RESEARCH.md
@.planning/phases/08-watermark-pipeline/08-01-SUMMARY.md

<interfaces>
<!-- n8n pipeline workflow structure from research -->

Pipeline step insertion point in WF1:
```
Existing:  file_detect -> exif_extract -> coverage_check -> lightroom_edit -> qa_gate -> packaging -> delivery
Updated:   file_detect -> exif_extract -> coverage_check -> lightroom_edit -> qa_gate -> watermark_preview -> packaging -> delivery
```

Supabase Storage upload pattern (n8n HTTP Request):
```
POST {{ $env.SUPABASE_URL }}/storage/v1/object/watermark-previews/{{ job_id }}/preview_01.jpg
Headers: apikey + Authorization: Bearer {{ $env.SUPABASE_SERVICE_KEY }}, Content-Type: image/jpeg
Body: Binary image data
```

Public URL pattern for previews:
```
{{ $env.SUPABASE_URL }}/storage/v1/object/public/watermark-previews/{{ job_id }}/preview_01.jpg
```

Database update pattern (n8n HTTP Request):
```
PATCH {{ $env.SUPABASE_URL }}/rest/v1/drone_jobs?id=eq.{{ job_id }}
Headers: apikey + Authorization: Bearer {{ $env.SUPABASE_SERVICE_KEY }}
Body: { "preview_urls": ["url1", "url2", "url3"] }
```

Image selection logic (Code node):
```javascript
const candidates = assets
  .filter(a => (a.processing_status === 'processed' || a.processing_status === 'exported') && a.file_type === 'image')
  .sort((a, b) => (b.qa_score || 0) - (a.qa_score || 0))
  .slice(0, 3);
```

ImageMagick watermark commands:
```bash
magick "input.jpg" -resize 1200x -strip "preview_base.jpg"
magick "preview_base.jpg" "watermark_tile.png" -gravity center -compose dissolve -define compose:args=35 -composite "preview_final.jpg"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate watermark tile PNG asset</name>
  <files>assets/watermark-tile.png</files>
  <action>
Create a tiled diagonal watermark PNG programmatically using the `canvas` npm package (already used in Phase 7 for PWA icon generation). The watermark specifications from research:

- Canvas size: 1200x800 pixels
- Background: Fully transparent (alpha = 0)
- Text: "SENTINEL AERIAL" repeated in a diagonal tiled pattern
- Text color: White with 35% opacity (rgba 255, 255, 255, 0.35)
- Rotation: 30 to 45 degrees diagonal
- Font: Bold sans serif, approximately 48px
- Spacing: Text repeated every ~300px horizontally and ~200px vertically to create a grid pattern
- Drop shadow: 2px offset in dark gray (rgba 0, 0, 0, 0.15) for visibility on light areas

The tile pattern must cover the entire 1200x800 canvas so that when composited over any preview image, no region is free of watermark text. This prevents cropping recovery.

Write a Node.js script (can be a temp script, does not need to be permanent) that generates the PNG and saves to `assets/watermark-tile.png`. Run the script. Verify the file exists and is a valid PNG.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && test -f assets/watermark-tile.png && node -e "const fs=require('fs'); const b=fs.readFileSync('assets/watermark-tile.png'); console.log(b[0]===0x89 && b[1]===0x50 ? 'VALID PNG' : 'INVALID')" && echo "Watermark tile exists and is valid PNG"</automated>
  </verify>
  <done>watermark-tile.png is a valid PNG with diagonal tiled "SENTINEL AERIAL" text pattern covering the full 1200x800 canvas. No region is free of watermark text.</done>
</task>

<task type="auto">
  <name>Task 2: Add watermark_preview step to n8n WF1 pipeline</name>
  <files>n8n-workflows/wf1-sentinel-pipeline-orchestrator.json</files>
  <action>
Read the existing `n8n-workflows/wf1-sentinel-pipeline-orchestrator.json`. Understand its node structure and connections.

Add a new watermark_preview step consisting of these n8n nodes, inserted AFTER the qa_gate step and BEFORE the packaging step:

Node 1: "Select Preview Images" (Code node)
- Fetches processed assets for the current job from Supabase (using HTTP Request to `/rest/v1/drone_assets?mission_id=eq.{job_id}&processing_status=in.(processed,exported)&file_type=eq.image&order=qa_score.desc&limit=3`)
- If no processed images found, skip watermark step (output empty array, connect to packaging directly)
- Output: array of up to 3 asset objects with file paths

Node 2: "Download Original" (HTTP Request node, loop)
- For each selected asset, download the original from drone-jobs bucket using service key
- URL: `{{ $env.SUPABASE_URL }}/storage/v1/object/drone-jobs/{{ $json.source_path }}`
- Headers: Authorization Bearer service key
- Response type: Binary

Node 3: "Resize and Watermark" (Edit Image node or Execute Command node)
- If using Edit Image node: Resize to max width 1200px, then composite with watermark-tile.png
- If Edit Image composite is not reliable: Use Execute Command node with ImageMagick commands:
  ```
  magick input.jpg -resize 1200x -strip preview_base.jpg
  magick preview_base.jpg /opt/sentinel/assets/watermark-tile.png -gravity center -compose dissolve -define compose:args=35 -composite preview_final.jpg
  ```
- CRITICAL: Include `-strip` flag to remove EXIF/GPS metadata from previews
- The watermark tile path in the Execute Command node MUST reference `/opt/sentinel/assets/watermark-tile.png` on the processing rig
- Output: Binary JPEG data

Node 4: "Upload Preview" (HTTP Request node, loop)
- Upload each watermarked JPEG to Supabase Storage
- URL: `{{ $env.SUPABASE_URL }}/storage/v1/object/watermark-previews/{{ $json.job_id }}/preview_0{{ $json.index }}.jpg`
- Method: POST
- Headers: apikey and Authorization Bearer service key, Content-Type image/jpeg
- Body: Binary data from previous node
- Use upsert header `x-upsert: true` to allow re-running the pipeline

Node 5: "Save Preview URLs" (HTTP Request node)
- PATCH `{{ $env.SUPABASE_URL }}/rest/v1/drone_jobs?id=eq.{{ $json.job_id }}`
- Body: `{ "preview_urls": [array of public URLs] }`
- Public URL format: `{{ $env.SUPABASE_URL }}/storage/v1/object/public/watermark-previews/{{ job_id }}/preview_01.jpg`

Connect the output of the watermark step to the existing packaging step. Ensure the pipeline continues to packaging/delivery regardless of whether watermark step produced 0, 1, 2, or 3 previews.

IMPORTANT: The n8n workflow JSON is the source of truth that gets imported into n8n. Update the JSON file in the repo. The user will import the updated workflow into their running n8n instance.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && grep -c "watermark" n8n-workflows/wf1-sentinel-pipeline-orchestrator.json && grep -c "watermark-previews" n8n-workflows/wf1-sentinel-pipeline-orchestrator.json && grep -c "preview_urls" n8n-workflows/wf1-sentinel-pipeline-orchestrator.json</automated>
  </verify>
  <done>WF1 pipeline JSON contains the watermark_preview step between qa_gate and packaging with all 5 nodes (select, download, resize/watermark, upload, save URLs). The Execute Command node references /opt/sentinel/assets/watermark-tile.png as the watermark tile path on the processing rig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify watermark pipeline end to end</name>
  <action>Human verifies watermark asset quality, migration results, admin UI compatibility, and n8n pipeline structure.</action>
  <what-built>Watermark pipeline infrastructure. Three Supabase migrations (watermark-previews public bucket, drone-jobs private bucket, preview_urls column). Updated n8n WF1 pipeline with watermark_preview step. Watermark tile PNG asset. The watermark tile must be deployed to the processing rig at /opt/sentinel/assets/watermark-tile.png for the n8n Execute Command node to use it.</what-built>
  <how-to-verify>
    1. Review the watermark-tile.png file visually. Open `assets/watermark-tile.png` in an image viewer. Verify the diagonal tiled "SENTINEL AERIAL" text covers the entire image with no large gaps. The text should be white with slight transparency on a transparent background.

    2. Apply migrations to Supabase. Run `supabase db push` or apply migrations manually. Verify in Supabase dashboard:
       a. Storage shows a new "watermark-previews" bucket marked as Public
       b. Storage shows "drone-jobs" bucket is now Private (not Public)

    3. Test admin QA modal still works. Open the admin UI, navigate to a job with assets, open the QA detail modal. Images should still display (using signed URLs or authenticated access).

    4. Import the updated WF1 workflow into n8n. Open n8n, import `wf1-sentinel-pipeline-orchestrator.json`. Verify the watermark_preview step appears between qa_gate and packaging with the correct node connections.

    5. Deploy watermark tile to processing rig. Copy `assets/watermark-tile.png` to `/opt/sentinel/assets/watermark-tile.png` on the rig (or update the path in the n8n Execute Command node to match your preferred location). This is a runtime dependency for the pipeline.

    6. (Optional) Trigger a test pipeline run on a job with processed images. After completion, check:
       a. 2 to 3 JPEG files exist in Supabase Storage under watermark-previews/{job_id}/
       b. Files are publicly accessible (no auth needed)
       c. The drone_jobs row has preview_urls populated
       d. Watermark text is visible on the preview images
  </how-to-verify>
  <verify>Visual inspection and manual testing per how-to-verify steps</verify>
  <done>Watermark asset approved and deployed to rig at /opt/sentinel/assets/watermark-tile.png. Migrations applied. n8n pipeline imported with watermark_preview step connected. Admin UI works with private bucket.</done>
  <resume-signal>Type "approved" if watermark quality and pipeline structure look correct, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- watermark-tile.png exists and is a valid PNG
- n8n WF1 JSON includes watermark_preview nodes with references to watermark-previews bucket and preview_urls
- Watermark tile path in n8n Execute Command references /opt/sentinel/assets/watermark-tile.png
- Migrations create correct bucket policies
- Full pipeline test produces watermarked previews in the correct bucket
</verification>

<success_criteria>
- After pipeline run, 2 to 3 watermarked preview JPEGs exist in watermark-previews/{job_id}/
- Preview URLs are publicly accessible without authentication
- Original file URLs in drone-jobs bucket return 400/403 without signed access
- Tiled diagonal watermark covers entire preview image and cannot be cropped out
- preview_urls column on drone_jobs is populated with public URLs
- Watermark tile is deployed to processing rig and referenced correctly by n8n
</success_criteria>

<output>
After completion, create `.planning/phases/08-watermark-pipeline/08-02-SUMMARY.md`
</output>
