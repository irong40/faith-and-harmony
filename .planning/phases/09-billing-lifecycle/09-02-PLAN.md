---
phase: 09-billing-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/square-webhook/index.ts
  - supabase/functions/send-payment-receipt-email/index.ts
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]

must_haves:
  truths:
    - "When balance payment is confirmed via Square webhook, drone_jobs.status updates to paid"
    - "After balance payment, webhook triggers receipt email and delivery email edge functions"
    - "Client receives a branded receipt email confirming balance payment"
  artifacts:
    - path: "supabase/functions/square-webhook/index.ts"
      provides: "Complete webhook with job status update and trigger chain"
      contains: "drone_jobs"
    - path: "supabase/functions/send-payment-receipt-email/index.ts"
      provides: "Receipt email after balance payment"
      min_lines: 60
  key_links:
    - from: "square-webhook"
      to: "drone_jobs.status"
      via: "Supabase update to paid"
      pattern: "update.*status.*paid"
    - from: "square-webhook"
      to: "send-payment-receipt-email"
      via: "Edge function fetch call"
      pattern: "functions/v1/send-payment-receipt-email"
    - from: "square-webhook"
      to: "drone-delivery-email"
      via: "Edge function fetch call"
      pattern: "functions/v1/drone-delivery-email"
---

<objective>
Extend the Square webhook to complete the billing lifecycle (job status update, receipt email, delivery release) and create the receipt email edge function.

Purpose: When a client pays their balance invoice via Square, the system automatically marks the job as paid, sends a receipt email, and releases full resolution deliverables without admin intervention.
Output: Modified webhook function, one new receipt email edge function.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-billing-lifecycle/09-RESEARCH.md

<interfaces>
<!-- Existing square-webhook structure (lines 162-195 are the payment update section) -->

From supabase/functions/square-webhook/index.ts:
```typescript
// After marking payment as paid (line 162-178), there is a TODO on line 182:
// TODO (Plan 04): Trigger receipt email when BOTH deposit and balance are paid
// if (payment.payment_type === 'balance') { ... trigger payment-receipt-email ... }
//
// The payment object at this point has: id, quote_id, status, payment_type, customer_email
// The function already returns a success response on line 185-194
```

From supabase/functions/drone-delivery-email/index.ts:
```typescript
// Accepts POST with { job_id, deliverable_ids?, custom_message?, download_url? }
// job_id is required
// Sends branded delivery email with download links
// Uses Resend for email delivery
```

From payments table:
```sql
-- payments.quote_id references quotes(id)
-- payments.job_id will exist after Plan 01 migration
-- drone_jobs has quote_id column for reverse lookup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Square webhook with job status update and trigger chain</name>
  <files>supabase/functions/square-webhook/index.ts</files>
  <action>
Modify the existing square-webhook edge function to add balance payment lifecycle handling. Replace the TODO comment on line 182 with actual implementation.

After the existing payment update to 'paid' status (line 162-178), add a balance payment handler:

1. Check if payment.payment_type === 'balance'. If not balance (it is a deposit payment), skip the trigger chain and return the existing success response.

2. For balance payments, find the drone_job. First try using payment.job_id (available after Plan 01 migration adds the column). Fall back to looking up via quote_id: query drone_jobs where quote_id equals payment.quote_id and select id.

3. If a job is found, update drone_jobs.status to 'paid' for that job ID.

4. Trigger the receipt email by calling send-payment-receipt-email edge function via fetch with SUPABASE_SERVICE_ROLE_KEY authorization. Pass { job_id: job.id, payment_id: payment.id }. Use the pattern: fetch(`${SUPABASE_URL}/functions/v1/send-payment-receipt-email`, { method: "POST", headers: { Authorization: `Bearer ${SUPABASE_SERVICE_ROLE_KEY}`, "Content-Type": "application/json" }, body: JSON.stringify(...) }).

5. Trigger delivery by calling drone-delivery-email edge function via fetch with the same auth pattern. Pass { job_id: job.id }.

6. Wrap both trigger calls in try/catch so that a failure in receipt or delivery does not cause the webhook to return an error to Square (Square would retry). Log errors but return 200.

7. Add job_id to the payment select query (line 133) so it is available: select("id, quote_id, job_id, status, payment_type, customer_email").

Keep all existing code (HMAC validation, event filtering, idempotency) unchanged. Only add the balance payment handler after the payment update.
  </action>
  <verify>
    <automated>grep -c "drone_jobs\|send-payment-receipt-email\|drone-delivery-email\|status.*paid\|payment_type.*balance" D:/Projects/FaithandHarmony/supabase/functions/square-webhook/index.ts | xargs test 4 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Webhook updates drone_jobs.status to paid for balance payments and triggers receipt email and delivery email edge functions</done>
</task>

<task type="auto">
  <name>Task 2: Receipt email edge function</name>
  <files>supabase/functions/send-payment-receipt-email/index.ts</files>
  <action>
Create the send-payment-receipt-email edge function following the Deno + CORS + serve() + Resend pattern.

1. Accept POST with { job_id, payment_id }.

2. Look up the payment by payment_id, selecting amount, payment_type, paid_at, customer_email, square_invoice_url.

3. Look up the drone_job by job_id, selecting quote_id. Then look up the quote to get quote_requests(name, job_type) and total, deposit_amount.

4. Use the BRAND object (same as drone-delivery-email and send-balance-due-email).

5. Build an HTML receipt email with Sentinel branding that includes:
   - Greeting with client name
   - "Payment received" confirmation message
   - Payment details: amount paid, payment date (formatted from paid_at), payment type (balance)
   - Job summary: job type, total project cost, deposit paid, balance paid
   - "Your deliverables are being prepared" message (since drone-delivery-email fires in parallel)
   - Sentinel footer with company info

6. Send via Resend from deliveries@sentinelaerialinspections.com to customer_email. Subject: "Payment Received - Sentinel Aerial Inspections".

7. Return { success: true, email_sent_to } on success.

Use standard CORS headers and error handling. If payment_id lookup fails, return 404. If Resend fails, return 502 with error details.
  </action>
  <verify>
    <automated>grep -c "Resend\|payment_id\|BRAND\|Payment.*received\|paid_at" D:/Projects/FaithandHarmony/supabase/functions/send-payment-receipt-email/index.ts | xargs test 4 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Receipt email edge function sends branded payment confirmation to client with payment details and job summary</done>
</task>

</tasks>

<verification>
- square-webhook TODO on line 182 replaced with working trigger chain
- Webhook updates drone_jobs.status to 'paid' for balance payments only
- Webhook calls send-payment-receipt-email and drone-delivery-email after balance payment
- Receipt email edge function sends via Resend with Sentinel branding
- Both trigger calls wrapped in try/catch to prevent webhook retry loops
- `npx vitest run` still passes (no regressions)
</verification>

<success_criteria>
- Balance payment webhook flow: mark paid -> update job status -> send receipt -> release deliverables
- Receipt email function deployable via `supabase functions deploy --use-api`
- Webhook trigger calls use service role key authorization
- Failures in downstream calls do not cause webhook to return non-200 to Square
</success_criteria>

<output>
After completion, create `.planning/phases/09-billing-lifecycle/09-02-SUMMARY.md`
</output>
