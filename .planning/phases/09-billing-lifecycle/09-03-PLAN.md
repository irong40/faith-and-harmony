---
phase: 09-billing-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/pages/admin/DroneJobDetail.tsx
  - src/pages/admin/components/PaymentsPanel.tsx
  - src/pages/admin/components/PaymentsPanel.spec.tsx
autonomous: false
requirements: [BILL-02, BILL-08]

must_haves:
  truths:
    - "Admin can trigger balance invoice creation from the job detail page via a button that calls the create-balance-invoice edge function"
    - "Admin payments panel shows deposit and balance rows per job with paid, pending, and overdue status badges"
  artifacts:
    - path: "src/pages/admin/DroneJobDetail.tsx"
      provides: "Balance invoice button on job detail page"
      contains: "create-balance-invoice"
    - path: "src/pages/admin/components/PaymentsPanel.tsx"
      provides: "Admin payments panel with deposit/balance status"
      min_lines: 80
    - path: "src/pages/admin/components/PaymentsPanel.spec.tsx"
      provides: "Unit tests for PaymentsPanel component"
      min_lines: 30
  key_links:
    - from: "DroneJobDetail.tsx"
      to: "create-balance-invoice"
      via: "supabase.functions.invoke"
      pattern: "create-balance-invoice"
    - from: "PaymentsPanel.tsx"
      to: "payments table"
      via: "Supabase query with job_id filter"
      pattern: "from.*payments"
---

<objective>
Add the admin UI components that complete the billing lifecycle: a balance invoice trigger button on the job detail page and a payments panel showing deposit/balance status per job.

Purpose: Admin needs to trigger balance invoicing from the job context and see payment status at a glance without checking Square directly.
Output: Modified DroneJobDetail page, new PaymentsPanel component with tests.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-billing-lifecycle/09-RESEARCH.md
@.planning/phases/09-billing-lifecycle/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: create-balance-invoice accepts { job_id } and returns { success, payment_id, square_invoice_id, square_invoice_url } -->
<!-- The payments table has: id, quote_id, job_id, payment_type (deposit|balance), status (pending|paid|overdue|waived), amount, square_invoice_url, customer_email, due_date, paid_at -->

From src/pages/admin/DroneJobDetail.tsx:
```typescript
// Line 250: export default function DroneJobDetail()
// Uses useParams() to get job ID
// Has existing function invoke patterns like:
//   supabase.functions.invoke("drone-delivery-email", { body: { job_id } })
//   supabase.functions.invoke("drone-job-token", { body: { job_id } })
// Uses Button from @/components/ui/button
// Has fetchJob() function that reloads job data
```

From existing admin components pattern:
```typescript
// Admin components use shadcn/ui (Badge, Table, Card, Button)
// TanStack Query v5 for data fetching
// Tailwind CSS for styling
// date-fns for date formatting
// lucide-react for icons
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentsPanel component with deposit/balance status</name>
  <files>src/pages/admin/components/PaymentsPanel.tsx, src/pages/admin/components/PaymentsPanel.spec.tsx</files>
  <behavior>
    - Test 1: Renders "No payments found" when payments array is empty
    - Test 2: Renders deposit and balance rows with correct amounts when both exist
    - Test 3: Shows "Paid" badge (green) for status === "paid"
    - Test 4: Shows "Pending" badge (yellow) for status === "pending"
    - Test 5: Shows "Overdue" badge (red) for status === "overdue"
    - Test 6: Shows Square invoice link when square_invoice_url is present
    - Test 7: Renders paid_at date when payment is paid
  </behavior>
  <action>
Create a PaymentsPanel component that displays payment status for a specific job.

Props: { jobId: string }

The component:
1. Queries the payments table filtering by job_id, selecting id, payment_type, status, amount, square_invoice_url, customer_email, due_date, paid_at, created_at. Order by payment_type ascending (balance after deposit).

2. Renders a Card with "Payments" heading.

3. For each payment row, display in a table or list layout:
   - Payment type label ("Deposit" or "Balance")
   - Amount formatted as USD currency
   - Status badge using shadcn Badge component: paid (green/default), pending (yellow/secondary), overdue (red/destructive), waived (gray/outline)
   - Due date (if balance type) formatted with date-fns
   - Paid date (if paid) formatted with date-fns
   - Square invoice link (external link icon) if square_invoice_url exists

4. Empty state: "No payments found" message when no payments exist for the job.

5. Use TanStack Query (useQuery) for data fetching with key ["payments", jobId].

Write the spec file first (TDD). Use @testing-library/react with a mock supabase client. Test the rendering of different payment states (paid, pending, overdue) and empty state.
  </action>
  <verify>
    <automated>npx vitest run src/pages/admin/components/PaymentsPanel.spec.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>PaymentsPanel renders deposit and balance rows with correct status badges, amounts, and dates. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Balance invoice button and PaymentsPanel on DroneJobDetail</name>
  <files>src/pages/admin/DroneJobDetail.tsx</files>
  <action>
Modify DroneJobDetail.tsx to add two billing UI elements:

1. Import and render the PaymentsPanel component, passing the job ID. Place it in the job detail layout after the existing job info sections (near the delivery and QA sections).

2. Add a "Send Balance Invoice" button that:
   - Only shows when job.status === 'complete' (processing finished, ready for billing)
   - Uses the same invoke pattern as existing buttons: `await supabase.functions.invoke("create-balance-invoice", { body: { job_id } })`
   - Shows loading state while the function executes
   - On success: shows a toast or alert with "Balance invoice sent" and refreshes the PaymentsPanel data (invalidate the TanStack Query key)
   - On error: shows error message from the response (especially the 409 "already exists" case)
   - Disable the button if a balance payment already exists for this job (check from PaymentsPanel data or a separate query)

3. Import the PaymentsPanel component from ./components/PaymentsPanel.

Place the button near other action buttons in the job detail page. Use the existing Button component from shadcn/ui. Use the DollarSign or Receipt icon from lucide-react.
  </action>
  <verify>
    <automated>grep -c "create-balance-invoice\|PaymentsPanel\|Send Balance" D:/Projects/FaithandHarmony/src/pages/admin/DroneJobDetail.tsx | xargs test 2 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DroneJobDetail page has a balance invoice button (visible when job status is complete) and displays the PaymentsPanel component showing deposit/balance status</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify billing UI</name>
  <action>Human verifies the balance invoice button and payments panel render correctly on the admin job detail page</action>
  <what-built>Balance invoice button on job detail page and payments panel showing deposit/balance status with paid, pending, and overdue badges</what-built>
  <how-to-verify>
    1. Navigate to an admin drone job detail page for a job with status "complete"
    2. Verify the "Send Balance Invoice" button is visible
    3. Verify the PaymentsPanel section appears showing any existing deposit payment
    4. If a deposit payment exists, verify it shows the correct amount and status badge
    5. Verify the button is hidden or disabled for jobs not in "complete" status
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- PaymentsPanel renders deposit and balance rows with status badges
- PaymentsPanel tests pass: `npx vitest run src/pages/admin/components/PaymentsPanel.spec.tsx`
- DroneJobDetail has balance invoice button gated on job status === 'complete'
- Button invokes create-balance-invoice edge function with job_id
- PaymentsPanel queries payments table by job_id
- `npx vitest run` full suite passes
</verification>

<success_criteria>
- Admin sees payments panel on job detail page with deposit/balance status
- Admin can trigger balance invoice from job detail page when job is complete
- Status badges correctly show paid (green), pending (yellow), overdue (red)
- PaymentsPanel spec tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-billing-lifecycle/09-03-SUMMARY.md`
</output>
