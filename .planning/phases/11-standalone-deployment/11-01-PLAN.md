---
phase: 11-standalone-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
autonomous: false
requirements:
  - DEPLOY-02
  - DEPLOY-03
user_setup:
  - service: vercel
    why: "Trestle standalone deployment"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Copy from local .env or existing Vercel project"
      - name: VITE_SUPABASE_PUBLISHABLE_KEY
        source: "Copy from local .env or existing Vercel project"
    dashboard_config:
      - task: "Create new Vercel project from FaithandHarmony repo"
        location: "Vercel Dashboard -> Add New -> Project -> Import FaithandHarmony repo"
      - task: "Add custom domain trestle.sentinelaerial.com"
        location: "Vercel Dashboard -> Project Settings -> Domains"
  - service: dns-provider
    why: "Subdomain CNAME record for trestle.sentinelaerial.com"
    dashboard_config:
      - task: "Add CNAME record: trestle -> cname.vercel-dns.com (TTL 3600)"
        location: "DNS provider managing sentinelaerial.com (check Vercel DNS settings for sentinel-landing project)"
  - service: supabase
    why: "Auth redirect URL allowlist for new subdomain"
    dashboard_config:
      - task: "Add redirect URLs for trestle.sentinelaerial.com"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"
must_haves:
  truths:
    - "trestle.sentinelaerial.com loads the Trestle PWA and is installable on mobile"
    - "Login at trestle.sentinelaerial.com works correctly with Supabase auth"
    - "Password reset and signup confirmation emails redirect to trestle.sentinelaerial.com"
    - "square-webhook edge function accepts POST without JWT requirement"
  artifacts:
    - path: "supabase/config.toml"
      provides: "square-webhook function config entry"
      contains: "[functions.square-webhook]"
  key_links:
    - from: "Vercel project"
      to: "trestle.sentinelaerial.com"
      via: "DNS CNAME record"
      pattern: "CNAME trestle -> cname.vercel-dns.com"
    - from: "Supabase auth"
      to: "trestle.sentinelaerial.com"
      via: "Redirect URL allowlist"
      pattern: "https://trestle.sentinelaerial.com/"
---

<objective>
Deploy Trestle as a standalone Vercel project at trestle.sentinelaerial.com and configure Supabase auth redirects for the new subdomain.

Purpose: The pilot portal needs its own subdomain separate from the sentinel-landing marketing site, with auth flows working correctly at the new domain.
Output: Live Trestle PWA at trestle.sentinelaerial.com with working authentication.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-standalone-deployment/11-RESEARCH.md

Key facts from research:
- Auth redirects already use `window.location.origin` so NO client code changes needed
- square-webhook function exists at supabase/functions/square-webhook/index.ts but is MISSING from config.toml
- vercel.json already has SPA rewrites and security headers
- PWA manifest already configured with Sentinel branding (Phase 7)
- Supabase project shared between sentinel-landing and Trestle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add square-webhook to config.toml and deploy edge functions</name>
  <files>supabase/config.toml</files>
  <action>
Add the missing `[functions.square-webhook]` entry with `verify_jwt = false` to `supabase/config.toml`. Place it after the last existing function entry (currently `[functions.respond-to-quote]`). This is the ONLY code change in the entire phase.

After adding the entry, run `npm test` and `npm run typecheck` to confirm no regressions. Then deploy edge functions using `supabase functions deploy square-webhook --use-api` (the --use-api flag is required on this machine per project conventions).

Do NOT modify any other file. Do NOT add hardcoded domain strings to client code.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && grep -A1 "functions.square-webhook" supabase/config.toml && npm test</automated>
  </verify>
  <done>config.toml contains [functions.square-webhook] with verify_jwt = false. Edge function deployed. All existing tests pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Vercel project, configure DNS, and set Supabase auth redirects</name>
  <what-built>Task 1 deployed the square-webhook config fix. Now the infrastructure configuration requires dashboard access that Claude cannot perform via CLI.</what-built>
  <how-to-verify>
Complete these steps in order:

**Step A: Create Vercel Project**
1. Go to vercel.com/dashboard -> Add New -> Project
2. Import the FaithandHarmony repository
3. Framework Preset should auto-detect Vite
4. Add environment variables before first deploy:
   - VITE_SUPABASE_URL (copy from local .env)
   - VITE_SUPABASE_PUBLISHABLE_KEY (copy from local .env)
   - VITE_WAYPOINTMAP_API_KEY (if used, copy from local .env)
5. Click Deploy and wait for build to succeed

**Step B: Add Custom Domain**
1. In the new Vercel project -> Settings -> Domains
2. Add: trestle.sentinelaerial.com
3. Vercel will show the required DNS record

**Step C: Add DNS CNAME Record**
1. Go to your DNS provider for sentinelaerial.com (check the sentinel-landing Vercel project Settings -> Domains to identify the provider)
2. Add CNAME record: Name=trestle, Value=cname.vercel-dns.com, TTL=3600
3. Wait for DNS propagation (usually 5 to 30 minutes)
4. Vercel will auto-provision the SSL certificate

**Step D: Add Supabase Auth Redirect URLs**
1. Go to Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs
2. Add these three exact URLs:
   - https://trestle.sentinelaerial.com/
   - https://trestle.sentinelaerial.com/auth
   - https://trestle.sentinelaerial.com/admin/settings
3. Save

**Verification:**
1. Visit https://trestle.sentinelaerial.com and confirm the app loads
2. Check that the PWA install prompt appears (test on mobile or Chrome DevTools Application tab)
3. Test login flow at the new domain
4. Test password reset flow (confirm reset email links to trestle.sentinelaerial.com, not localhost)
  </how-to-verify>
  <resume-signal>Type "deployed" when trestle.sentinelaerial.com loads and login works, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- `grep -A1 "functions.square-webhook" supabase/config.toml` shows `verify_jwt = false`
- `npm test` passes with no regressions
- trestle.sentinelaerial.com loads the Trestle PWA (manual)
- Login/signup/reset flows work at the new domain (manual)
- PWA is installable from the subdomain (manual)
</verification>

<success_criteria>
- config.toml updated with square-webhook entry
- Trestle PWA loads at trestle.sentinelaerial.com
- Supabase auth redirect URLs configured for the subdomain
- Login, signup, and password reset flows work at the new domain
</success_criteria>

<output>
After completion, create `.planning/phases/11-standalone-deployment/11-01-SUMMARY.md`
</output>
