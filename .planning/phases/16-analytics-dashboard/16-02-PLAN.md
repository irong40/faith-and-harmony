---
phase: 16-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/components/admin/LeadStatsHeader.tsx
  - src/pages/admin/Leads.tsx
autonomous: true
requirements: [ANLY-01, ANLY-02, ANLY-03, ANLY-04]

must_haves:
  truths:
    - "Admin sees a conversion rate stat card with percentage and week/month/all toggle on the leads page"
    - "Admin sees a leads by source stat card showing counts per source_channel for the selected window"
    - "Admin sees an average response time stat card showing mean hours between lead creation and first activity"
    - "Admin sees a revenue from leads stat card showing total dollars from jobs linked to converted leads"
    - "Stat cards update when the time window toggle changes"
  artifacts:
    - path: "src/components/admin/LeadStatsHeader.tsx"
      provides: "Stat cards header component for leads page"
      exports: ["LeadStatsHeader"]
      min_lines: 60
    - path: "src/pages/admin/Leads.tsx"
      provides: "Updated leads page with stats header"
      contains: "LeadStatsHeader"
  key_links:
    - from: "src/components/admin/LeadStatsHeader.tsx"
      to: "src/hooks/useLeadStats.ts"
      via: "useLeadStats hook"
      pattern: "useLeadStats"
    - from: "src/pages/admin/Leads.tsx"
      to: "src/components/admin/LeadStatsHeader.tsx"
      via: "component import"
      pattern: "LeadStatsHeader"
---

<objective>
Build a LeadStatsHeader component displaying 4 analytics stat cards with a time window toggle, then integrate it into the existing Leads admin page above the tabs.

Purpose: Give admin an immediate visual read on lead conversion performance, source mix, response speed, and revenue attribution without scrolling or clicking.

Output: One new component, one existing page updated with the header.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/admin/Leads.tsx
@src/hooks/useLeadStats.ts
@src/components/ui/card.tsx
@src/components/ui/badge.tsx

<interfaces>
From src/hooks/useLeadStats.ts (created in Plan 01):
```typescript
type TimeWindow = "week" | "month" | "all";
type LeadStats = {
  time_window: TimeWindow;
  conversion: { total: number; converted: number; rate: number };
  by_source: Array<{ source: string; count: number }>;
  response_time: { avg_hours: number };
  revenue: { total_revenue: number };
};
export function useLeadStats(timeWindow: TimeWindow): UseQueryResult<LeadStats>;
```

From src/pages/admin/Leads.tsx existing structure:
- Uses Tabs component for "Voice Leads" and "Drone Leads" tabs
- Has Card, Badge, Button, and other shadcn components imported
- SOURCE_CHANNEL_LABELS maps source_channel values to display names
- SOURCE_CHANNEL_COLORS maps source_channel values to badge color classes

Existing UI patterns (from admin Dashboard.tsx and other admin pages):
- Card with CardHeader (CardTitle, CardDescription) and CardContent
- Responsive grids using Tailwind: grid gap-4 md:grid-cols-2 lg:grid-cols-4
- Amber and teal accent colors throughout the app
- Loader2 spinner from lucide-react for loading states
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LeadStatsHeader component</name>
  <files>src/components/admin/LeadStatsHeader.tsx</files>
  <action>
Create `src/components/admin/LeadStatsHeader.tsx`:

A component that renders a time window toggle and 4 stat cards in a responsive grid.

**Time window toggle:**
Use three Button components (not Tabs, to keep it compact) with variant="outline" for inactive and variant="default" for active. Labels: "Week", "Month", "All Time". Default to "month".

**Card 1: Conversion Rate (ANLY-01)**
- Title: "Conversion Rate"
- Large number: "{rate}%"
- Description line: "{converted} of {total} leads"
- If total is 0, show "No leads yet" as description

**Card 2: Leads by Source (ANLY-02)**
- Title: "Leads by Source"
- List each source with count, sorted by count descending
- Each row: source label from SOURCE_CHANNEL_LABELS on left, count on right
- Use the SOURCE_CHANNEL_COLORS for small dot indicators
- If no data, show "No leads yet"

**Card 3: Average Response Time (ANLY-03)**
- Title: "Avg Response Time"
- Large number: format as "{X.X}h" if under 48 hours, "{X.X}d" if 48+ hours
- Color coding: text-green-600 if under 24h, text-amber-500 if 24 to 48h, text-red-500 if over 48h
- Description: "Hours to first contact"
- If 0 and no leads, show a dash

**Card 4: Revenue from Leads (ANLY-04)**
- Title: "Lead Revenue"
- Large number: "$X,XXX.XX" formatted with toLocaleString
- Description: "From converted leads"
- Color: text-green-600

**Loading state:** Show Loader2 spinner centered in the grid area while data loads.

**Error state:** Show nothing (fail silently, the leads table still works).

Import SOURCE_CHANNEL_LABELS and SOURCE_CHANNEL_COLORS from the Leads page or define locally (check if they are exported). If not exported, define a local copy in this component file.

Follow project CLAUDE.md writing constraints in any user visible text. No dashes, no semicolons, no emojis.
  </action>
  <verify>
    <automated>cd "D:/Projects/FaithandHarmony" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>LeadStatsHeader renders 4 stat cards with time window toggle. Uses useLeadStats hook. Responsive grid layout. Loading and empty states handled.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate LeadStatsHeader into Leads page</name>
  <files>src/pages/admin/Leads.tsx</files>
  <action>
Edit `src/pages/admin/Leads.tsx` to add the LeadStatsHeader above the existing Tabs section.

1. Add import at top:
```typescript
import { LeadStatsHeader } from "@/components/admin/LeadStatsHeader";
```

2. Find the main return JSX. The page currently has a structure like:
```
<div>
  <AdminNav />
  <main>
    <h1>Leads</h1>
    ... search/filter controls ...
    <Tabs>
      <TabsList>Voice Leads / Drone Leads</TabsList>
      ...
    </Tabs>
  </main>
</div>
```

Insert `<LeadStatsHeader />` between the page heading area and the Tabs component. Add a `mb-6` margin below it for spacing.

Do NOT restructure the existing page layout. Just insert the component in the right spot.

If SOURCE_CHANNEL_LABELS and SOURCE_CHANNEL_COLORS are not exported from Leads.tsx, either export them (preferred) or define them locally in LeadStatsHeader.tsx.
  </action>
  <verify>
    <automated>cd "D:/Projects/FaithandHarmony" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Leads page renders LeadStatsHeader above the tabs. Stats cards visible on page load. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- LeadStatsHeader component exists with 4 stat cards
- Leads.tsx imports and renders LeadStatsHeader
- Time window toggle has week/month/all options
- Each card maps to its ANLY requirement
</verification>

<success_criteria>
- Conversion rate card shows percentage with lead counts
- Source breakdown card shows per channel counts using source_channel labels
- Response time card shows formatted hours/days with color coding
- Revenue card shows dollar amount from drone_jobs linked through converted leads
- Time window toggle updates all cards on selection change
- Stats header appears above the Voice Leads / Drone Leads tabs
</success_criteria>

<output>
After completion, create `.planning/phases/16-analytics-dashboard/16-02-SUMMARY.md`
</output>
