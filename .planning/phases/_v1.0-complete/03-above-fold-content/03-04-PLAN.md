---
phase: 03-above-fold-content
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/components/landing/PortfolioGrid.tsx
  - src/pages/LandingPage.tsx
  - src/pages/landing.css
autonomous: true
requirements:
  - PAGE-06
  - CONV-05

must_haves:
  truths:
    - "The portfolio section renders inline on the page with visible photos"
    - "Six photos are displayed using the creek-after, dock-after, and trees-after images plus their before counterparts"
    - "Each photo has a service type label visible on or below the image"
    - "The section does not link out to an external gallery as the primary interaction"
    - "All images have descriptive alt text, explicit width and height attributes, and loading=lazy"
  artifacts:
    - path: "src/components/landing/PortfolioGrid.tsx"
      provides: "Inline photo grid with service type labels"
      min_lines: 60
    - path: "src/pages/landing.css"
      provides: "CSS for portfolio grid layout and photo labels"
      contains: ".lp-portfolio-grid"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/landing/PortfolioGrid.tsx"
      via: "import and render replacing the existing lp-portfolio section"
      pattern: "PortfolioGrid"
    - from: "PortfolioGrid.tsx img elements"
      to: "/assets/aerial/*.png and /assets/aerial/*.jpg"
      via: "src attribute with correct public path"
      pattern: "assets/aerial"
---

<objective>
Build an inline portfolio grid that shows the 6 available aerial photos with service type labels directly on the landing page, replacing the current external gallery link.

Purpose: The current portfolio section sends visitors away from the page via an external link. An inline grid keeps visitors engaged and demonstrates work without requiring navigation.
Output: PortfolioGrid.tsx component, CSS for the grid layout and label overlay, LandingPage.tsx updated to use the component.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
@D:/Projects/FaithandHarmony/src/pages/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PortfolioGrid component</name>
  <files>src/components/landing/PortfolioGrid.tsx</files>
  <action>
    Create PortfolioGrid.tsx as a React functional component.

    Define a typed array of portfolio items above the component. Each item is an object with these fields:
    - src: string (the image path)
    - alt: string (descriptive alt text)
    - label: string (service type shown on the photo)
    - width: number
    - height: number

    Use these 6 items in this order:

    1. src: "/assets/aerial/creek-after.png", alt: "Aerial view of creek property after editing, Hampton Roads VA", label: "Residential Listing", width: 1200, height: 800
    2. src: "/assets/aerial/dock-after.png", alt: "Aerial view of waterfront dock property after editing, Hampton Roads VA", label: "Residential Listing", width: 1200, height: 800
    3. src: "/assets/aerial/trees-after.png", alt: "Aerial view of wooded property after editing, Hampton Roads VA", label: "Residential Listing", width: 1200, height: 800
    4. src: "/assets/aerial/creek-before.jpg", alt: "Aerial view of creek property, Hampton Roads VA", label: "Property Overview", width: 1200, height: 800
    5. src: "/assets/aerial/dock-before.jpg", alt: "Aerial view of waterfront dock, Hampton Roads VA", label: "Property Overview", width: 1200, height: 800
    6. src: "/assets/aerial/trees-before.jpg", alt: "Aerial view of wooded property, Hampton Roads VA", label: "Property Overview", width: 1200, height: 800

    Note: The actual pixel dimensions of the aerial photos are unknown at plan time. Use width: 1200, height: 800 as reasonable defaults that match a typical aerial photo aspect ratio. Phase 5 (image optimization) will verify and correct these if needed.

    The component renders a section element with className "lp-portfolio" and id="portfolio".

    Inside a div with className "lp-container":

    First child: H2 with className "lp-section-title" and text "Our Work"

    Second child: p with className "lp-portfolio-grid__subtitle" and text "Aerial photography and property documentation across Hampton Roads. All photos captured with FAA Part 107 certified pilots."

    Writing constraints: no dashes, no marketing language.

    Third child: div with className "lp-portfolio-grid__grid". Map over the items array and render each item as a div with className "lp-portfolio-grid__item":
    - img element with src, alt, width, height, and loading="lazy"
    - div with className "lp-portfolio-grid__label" containing the label text

    Do NOT include the external gallery link as the primary CTA. The portfolio is inline. Remove the external sentinelaerial.faithandharmonyllc.com link that was in the original section.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm PortfolioGrid.tsx exists at src/components/landing/PortfolioGrid.tsx and compiles without errors. Confirm all 6 image paths reference /assets/aerial/ correctly.</manual>
  </verify>
  <done>PortfolioGrid.tsx exists, compiles cleanly, maps 6 items with correct image paths from /assets/aerial/, and each item has alt text, width, height, and loading="lazy".</done>
</task>

<task type="auto">
  <name>Task 2: Add portfolio grid CSS and wire into LandingPage</name>
  <files>src/pages/landing.css, src/pages/LandingPage.tsx</files>
  <action>
    In landing.css, append portfolio grid styles after the pricing block. Do not change existing styles. The existing .lp-portfolio selector already exists in the file and styles the section background and padding. Do not redeclare those rules. Add only the new sub-element selectors.

    .landing-page .lp-portfolio-grid__subtitle
    - font-family: 'Share Tech Mono', monospace
    - font-size: 14px
    - color: var(--steel-gray)
    - text-align: center
    - margin-bottom: 48px
    - max-width: 700px
    - margin-left: auto
    - margin-right: auto
    - line-height: 1.9

    .landing-page .lp-portfolio-grid__grid
    - display: grid
    - grid-template-columns: repeat(3, 1fr)
    - gap: 16px

    .landing-page .lp-portfolio-grid__item
    - position: relative
    - overflow: hidden
    - border: 1px solid rgba(255, 107, 53, 0.15)
    - transition: border-color 0.3s ease

    .landing-page .lp-portfolio-grid__item:hover
    - border-color: var(--safety-orange)

    .landing-page .lp-portfolio-grid__item img
    - width: 100%
    - height: 280px
    - object-fit: cover
    - display: block
    - transition: transform 0.4s ease

    .landing-page .lp-portfolio-grid__item:hover img
    - transform: scale(1.04)

    .landing-page .lp-portfolio-grid__label
    - position: absolute
    - bottom: 0
    - left: 0
    - right: 0
    - padding: 12px 16px
    - background: linear-gradient(0deg, rgba(5, 5, 5, 0.9) 0%, transparent 100%)
    - font-family: 'Share Tech Mono', monospace
    - font-size: 11px
    - color: var(--warm-white)
    - letter-spacing: 2px
    - text-transform: uppercase

    In LandingPage.tsx:

    1. Import PortfolioGrid: import PortfolioGrid from '../components/landing/PortfolioGrid'
    2. Find the existing lp-portfolio section (the one with id="portfolio" that links to the external gallery). Replace it entirely with the PortfolioGrid component: <PortfolioGrid />
    3. The PortfolioGrid component already has id="portfolio" on its section element so the sticky nav anchor will work.
    4. Remove the old lp-portfolio JSX block completely. Do not leave it commented out.

    Do not change any other JSX.
  </action>
  <verify>
    <automated>cd D:/Projects/FaithandHarmony && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
    <manual>Run npm run dev. Scroll to portfolio section. Confirm 6 photos appear in a 3-column grid. Confirm each photo has a visible service type label. Confirm no external gallery link replaces the grid. Confirm images load from /assets/aerial/. Check browser network tab to verify images are requested from the correct paths.</manual>
  </verify>
  <done>Portfolio section renders 6 inline photos in a grid with service type labels. No external gallery link is the primary interaction. All images have alt text, width, height, and loading="lazy". Build succeeds.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors: npx tsc --noEmit
- Vite build succeeds: npm run build
- PortfolioGrid.tsx exists at src/components/landing/PortfolioGrid.tsx
- All 6 images reference /assets/aerial/ paths
- Each img has alt, width, height, and loading="lazy"
- The external sentinelaerial.faithandharmonyllc.com gallery link is removed from this section
</verification>

<success_criteria>
PAGE-06: Inline portfolio grid shows 6 photos with service type labels.
CONV-05: Portfolio section is embedded inline on the page, not an external link only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-above-fold-content/03-04-SUMMARY.md` using the summary template.
</output>
