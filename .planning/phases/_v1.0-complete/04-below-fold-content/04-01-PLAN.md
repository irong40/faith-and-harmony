---
phase: 04-below-fold-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/MilitaryAirspace.tsx
  - src/pages/LandingPage.tsx
  - src/pages/landing.css
autonomous: true
requirements:
  - PAGE-07

must_haves:
  truths:
    - "The page contains a section with the text 'Norfolk Naval Station', 'NAS Oceana', and 'Langley Air Force Base'"
    - "The section explains that Sentinel holds LAANC authorization for controlled airspace near all three installations"
    - "The section connects the founder's military background to the airspace competency"
    - "The section heading or subheading names at least two of the three installations"
  artifacts:
    - path: "src/components/landing/MilitaryAirspace.tsx"
      provides: "Military airspace differentiator section"
      exports: ["MilitaryAirspace"]
    - path: "src/pages/LandingPage.tsx"
      provides: "Renders MilitaryAirspace in page flow after portfolio section"
      contains: "MilitaryAirspace"
    - path: "src/pages/landing.css"
      provides: "Scoped styles for .lp-airspace-* classes"
      contains: ".lp-airspace"
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/landing/MilitaryAirspace.tsx"
      via: "import and JSX render"
      pattern: "MilitaryAirspace"
---

<objective>
Build the military airspace differentiator section. This section explains why flying near Hampton Roads military installations requires expertise that most drone operators do not have. It names the three installations directly and ties the LAANC authorization capability to the founder's military background.

Purpose: No competitor in Hampton Roads claims this positioning. Naming the installations creates keyword density for "drone photography near NAS Oceana" and similar long tail searches while giving real estate agents and contractors confidence that Sentinel can fly jobs near restricted areas.

Output: A MilitaryAirspace component rendered in LandingPage.tsx with matching scoped CSS in landing.css.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/FaithandHarmony/.planning/PROJECT.md
@D:/Projects/FaithandHarmony/.planning/ROADMAP.md
@D:/Projects/FaithandHarmony/CLAUDE.md
@D:/Projects/FaithandHarmony/src/pages/LandingPage.tsx
@D:/Projects/FaithandHarmony/src/pages/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MilitaryAirspace component</name>
  <files>src/components/landing/MilitaryAirspace.tsx</files>
  <action>
    Create src/components/landing/MilitaryAirspace.tsx. The directory src/components/landing/ may need to be created.

    The component renders a section with aria-label="Military airspace authorization" and className="lp-airspace".

    Structure:

    ```tsx
    <section className="lp-airspace" aria-label="Military airspace authorization">
      <div className="lp-container">
        <h2 className="lp-section-title">FLYING IN RESTRICTED AIRSPACE</h2>
        <p className="lp-airspace-lead">[lead paragraph]</p>
        <div className="lp-airspace-grid">
          [three installation cards]
        </div>
        <p className="lp-airspace-cta-text">[closing statement]</p>
      </div>
    </section>
    ```

    Lead paragraph text (write in CLAUDE.md voice, no dashes or semicolons, active voice):
    "Hampton Roads has some of the most complex restricted airspace in the country. Norfolk Naval Station, the world's largest naval base, sits at the heart of the service area. NAS Oceana serves as the master jet base for the Atlantic Fleet. Langley Air Force Base anchors the eastern edge. Most drone operators avoid these corridors entirely. Sentinel flies them routinely."

    Three installation cards, each with className="lp-airspace-card":

    Card 1:
    - h3: "NORFOLK NAVAL STATION"
    - p: "The world's largest naval base. Class D and Class C airspace surrounds the installation. LAANC authorization is required for any commercial flight within the grid. Sentinel maintains active authorization."

    Card 2:
    - h3: "NAS OCEANA"
    - p: "Master jet base for the Atlantic Fleet. F-18 training operations run year round. The airspace surrounding Oceana is among the most actively managed in the country. Every flight near this installation requires coordination and LAANC approval."

    Card 3:
    - h3: "LANGLEY AIR FORCE BASE"
    - p: "Home to the 1st Fighter Wing and Air Combat Command. The airspace extends across Hampton and the surrounding area. Sentinel has operated in this corridor since launch."

    Closing statement paragraph with className="lp-airspace-cta-text":
    "Dr. Pierce flew missions as an Army Captain for nine years. The planning discipline that comes with that background is not something you learn from a weekend certification course. Your property gets the same level of pre-mission analysis that a military operation would receive."

    Export default MilitaryAirspace.

    Writing rule check before writing: no dashes, no semicolons, no colons in prose, no marketing language. All prose above follows these rules.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10</automated>
    <manual>Confirm component file exists and contains "Norfolk Naval Station", "NAS Oceana", and "Langley Air Force Base" text.</manual>
  </verify>
  <done>MilitaryAirspace.tsx compiles without TypeScript errors. Contains all three installation names in prose.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MilitaryAirspace into LandingPage and add CSS</name>
  <files>
    src/pages/LandingPage.tsx
    src/pages/landing.css
  </files>
  <action>
    In src/pages/LandingPage.tsx:

    Import MilitaryAirspace from '@/components/landing/MilitaryAirspace' (or relative path '../components/landing/MilitaryAirspace' depending on tsconfig paths).

    Place the MilitaryAirspace component after the portfolio section (lp-portfolio) and before the about section (lp-about). The existing about section will be replaced in plan 04-04 but leave it in place for now. Insert MilitaryAirspace between them:

    ```tsx
    {/* Portfolio */}
    <section className="lp-portfolio" id="portfolio">
      ...
    </section>

    <MilitaryAirspace />

    {/* About */}
    <section className="lp-about">
      ...
    </section>
    ```

    In src/pages/landing.css:

    Append the following new rules at the end of the file (after the existing responsive block). All class names use the .lp-airspace- prefix and are scoped under .landing-page:

    ```css
    /* Military Airspace Section */
    .landing-page .lp-airspace {
      padding: 100px 0;
      background: linear-gradient(180deg, var(--gunmetal) 0%, var(--deep-black) 100%);
      position: relative;
    }

    .landing-page .lp-airspace::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--safety-orange), transparent);
    }

    .landing-page .lp-airspace-lead {
      font-family: 'Share Tech Mono', monospace;
      font-size: 15px;
      color: var(--warm-white);
      max-width: 900px;
      margin: 0 auto 70px;
      line-height: 1.9;
      text-align: center;
      letter-spacing: 0.3px;
    }

    .landing-page .lp-airspace-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 40px;
      margin-bottom: 70px;
    }

    .landing-page .lp-airspace-card {
      background: rgba(28, 28, 28, 0.8);
      padding: 40px;
      border: 1px solid rgba(255, 107, 53, 0.2);
      border-top: 3px solid var(--safety-orange);
      transition: all 0.3s ease;
    }

    .landing-page .lp-airspace-card:hover {
      border-color: var(--safety-orange);
      box-shadow: 0 8px 30px rgba(255, 107, 53, 0.2);
      transform: translateY(-4px);
    }

    .landing-page .lp-airspace-card h3 {
      font-family: 'Saira Condensed', sans-serif;
      font-size: 24px;
      font-weight: 900;
      color: var(--safety-orange);
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 20px;
    }

    .landing-page .lp-airspace-card p {
      font-family: 'Share Tech Mono', monospace;
      font-size: 13px;
      color: var(--steel-gray);
      line-height: 1.9;
      letter-spacing: 0.3px;
    }

    .landing-page .lp-airspace-cta-text {
      font-family: 'Share Tech Mono', monospace;
      font-size: 14px;
      color: var(--steel-gray);
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
      line-height: 1.9;
    }

    @media (max-width: 768px) {
      .landing-page .lp-airspace-grid {
        grid-template-columns: 1fr;
      }
    }
    ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
    <manual>Run npm run dev, visit http://localhost:5173. Scroll to the section below the portfolio. Confirm three installation cards appear with correct headings. Confirm no dashes appear in any visible text.</manual>
  </verify>
  <done>MilitaryAirspace renders in page flow. Build passes. Section contains the three named installations. No TypeScript or CSS errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npm run typecheck` passes
3. Rendered page DOM contains "Norfolk Naval Station", "NAS Oceana", "Langley Air Force Base"
4. No prose text in the section contains a dash character, semicolon, colon, or emoji
5. CSS classes follow .lp-airspace- prefix and are scoped under .landing-page
</verification>

<success_criteria>
- MilitaryAirspace component exists and renders three installation cards
- All three installation names appear in visible page text
- Component positioned after portfolio section in LandingPage.tsx
- CSS appended to landing.css with proper scoping
- Build passes
</success_criteria>

<output>
After completion, create .planning/phases/04-below-fold-content/04-01-SUMMARY.md with:
- Component file path and export name
- Text content used for each installation card
- CSS class names added to landing.css
- Any deviations from the plan
</output>
